import Omi.Wire

/-!
# National Association of Securities Dealers Automated Quotations (Nasdaq) Snapshot v3.0

Generated from the binary model, with the proofs the model's rules call for: every record
decodes back to what was encoded; a message dispatch selects the message its type names;
a count is written from the list it counts; a length prefix is written from the bytes it frames;
and a packet read to the end of its data decodes to the messages that were written.

Text fields are kept byte for byte, padding included, so what is decoded encodes back unchanged.
Prices with implied decimals are proven as the integers on the wire.
-/

namespace Omi.NasdaqUtpSnapshotUtpV30Server

/-- Market Center Originator: one byte code -/
def MarketCenterOriginator.codes : List UInt8 :=
  [0x59, 0x5A, 0x4A, 0x4B, 0x57, 0x42, 0x58, 0x51, 0x49, 0x4E, 0x50, 0x41, 0x43, 0x4D, 0x46, 0x47, 0x44, 0x56, 0x4C, 0x48, 0x55, 0x45]

inductive MarketCenterOriginator where
  | cboeByxExchangeInc -- Cboe Byx Exchange Inc
  | cboeBzxExchangeInc -- Cboe Bzx Exchange Inc
  | cboeEdgaExchangeInc -- Cboe Edga Exchange Inc
  | cboeEdgxExchangeInc -- Cboe Edgx Exchange Inc
  | cboeExchangeInc -- Cboe Exchange Inc
  | nasdaqTexasLlc -- Nasdaq Texas Llc
  | nasdaqPhlxLlc -- Nasdaq Phlx Llc
  | nasdaqInc -- Nasdaq Inc
  | nasdaqIseLlc -- Nasdaq Ise Llc
  | newYorkStockExchangeLlc -- New York Stock Exchange Llc
  | nyseArcaInc -- Nyse Arca Inc
  | nyseAmericanLlc -- Nyse American Llc
  | nyseNationalInc -- Nyse National Inc
  | nyseTexas -- Nyse Texas
  | texasStockExchangeLlc -- Texas Stock Exchange Llc
  | n24XNationalExchangeLlc -- N 24 X National Exchange Llc
  | financialIndustryRegulatoryAuthority -- Financial Industry Regulatory Authority
  | investorsExchangeLlc -- Investors Exchange Llc
  | longTermStockExchange -- Long Term Stock Exchange
  | miaxPearlLlc -- Miax Pearl Llc
  | memxLlc -- Memx Llc
  | marketIndependentGeneratedBySip -- Market Independent Generated By Sip
  | unlisted (byte : { byte : UInt8 // byte ∉ MarketCenterOriginator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace MarketCenterOriginator

def toByte : MarketCenterOriginator → UInt8
  | .cboeByxExchangeInc => 0x59
  | .cboeBzxExchangeInc => 0x5A
  | .cboeEdgaExchangeInc => 0x4A
  | .cboeEdgxExchangeInc => 0x4B
  | .cboeExchangeInc => 0x57
  | .nasdaqTexasLlc => 0x42
  | .nasdaqPhlxLlc => 0x58
  | .nasdaqInc => 0x51
  | .nasdaqIseLlc => 0x49
  | .newYorkStockExchangeLlc => 0x4E
  | .nyseArcaInc => 0x50
  | .nyseAmericanLlc => 0x41
  | .nyseNationalInc => 0x43
  | .nyseTexas => 0x4D
  | .texasStockExchangeLlc => 0x46
  | .n24XNationalExchangeLlc => 0x47
  | .financialIndustryRegulatoryAuthority => 0x44
  | .investorsExchangeLlc => 0x56
  | .longTermStockExchange => 0x4C
  | .miaxPearlLlc => 0x48
  | .memxLlc => 0x55
  | .marketIndependentGeneratedBySip => 0x45
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : MarketCenterOriginator :=
  if byte = 0x59 then .cboeByxExchangeInc
  else if byte = 0x5A then .cboeBzxExchangeInc
  else if byte = 0x4A then .cboeEdgaExchangeInc
  else if byte = 0x4B then .cboeEdgxExchangeInc
  else if byte = 0x57 then .cboeExchangeInc
  else if byte = 0x42 then .nasdaqTexasLlc
  else if byte = 0x58 then .nasdaqPhlxLlc
  else if byte = 0x51 then .nasdaqInc
  else if byte = 0x49 then .nasdaqIseLlc
  else if byte = 0x4E then .newYorkStockExchangeLlc
  else if byte = 0x50 then .nyseArcaInc
  else if byte = 0x41 then .nyseAmericanLlc
  else if byte = 0x43 then .nyseNationalInc
  else if byte = 0x4D then .nyseTexas
  else if byte = 0x46 then .texasStockExchangeLlc
  else if byte = 0x47 then .n24XNationalExchangeLlc
  else if byte = 0x44 then .financialIndustryRegulatoryAuthority
  else if byte = 0x56 then .investorsExchangeLlc
  else if byte = 0x4C then .longTermStockExchange
  else if byte = 0x48 then .miaxPearlLlc
  else if byte = 0x55 then .memxLlc
  else .marketIndependentGeneratedBySip

def ofByte (byte : UInt8) : MarketCenterOriginator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : MarketCenterOriginator) : ofByte value.toByte = value := by
  cases value with
  | cboeByxExchangeInc => decide
  | cboeBzxExchangeInc => decide
  | cboeEdgaExchangeInc => decide
  | cboeEdgxExchangeInc => decide
  | cboeExchangeInc => decide
  | nasdaqTexasLlc => decide
  | nasdaqPhlxLlc => decide
  | nasdaqInc => decide
  | nasdaqIseLlc => decide
  | newYorkStockExchangeLlc => decide
  | nyseArcaInc => decide
  | nyseAmericanLlc => decide
  | nyseNationalInc => decide
  | nyseTexas => decide
  | texasStockExchangeLlc => decide
  | n24XNationalExchangeLlc => decide
  | financialIndustryRegulatoryAuthority => decide
  | investorsExchangeLlc => decide
  | longTermStockExchange => decide
  | miaxPearlLlc => decide
  | memxLlc => decide
  | marketIndependentGeneratedBySip => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : MarketCenterOriginator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (MarketCenterOriginator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : MarketCenterOriginator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : MarketCenterOriginator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end MarketCenterOriginator

/-- Sub Market Center Id: one byte code -/
def SubMarketCenterId.codes : List UInt8 :=
  [0x4E, 0x51, 0x42, 0x20]

inductive SubMarketCenterId where
  | finraNyseTrf -- Finra Nyse Trf
  | finraNasdaqTrfCarteret -- Finra Nasdaq Trf Carteret
  | finraNasdaqTrfChicago -- Finra Nasdaq Trf Chicago
  | tradeOriginatesFromFinraAlternativeDisplayFacility -- Trade Originates From Finra Alternative Display Facility
  | unlisted (byte : { byte : UInt8 // byte ∉ SubMarketCenterId.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace SubMarketCenterId

def toByte : SubMarketCenterId → UInt8
  | .finraNyseTrf => 0x4E
  | .finraNasdaqTrfCarteret => 0x51
  | .finraNasdaqTrfChicago => 0x42
  | .tradeOriginatesFromFinraAlternativeDisplayFacility => 0x20
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : SubMarketCenterId :=
  if byte = 0x4E then .finraNyseTrf
  else if byte = 0x51 then .finraNasdaqTrfCarteret
  else if byte = 0x42 then .finraNasdaqTrfChicago
  else .tradeOriginatesFromFinraAlternativeDisplayFacility

def ofByte (byte : UInt8) : SubMarketCenterId :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : SubMarketCenterId) : ofByte value.toByte = value := by
  cases value with
  | finraNyseTrf => decide
  | finraNasdaqTrfCarteret => decide
  | finraNasdaqTrfChicago => decide
  | tradeOriginatesFromFinraAlternativeDisplayFacility => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : SubMarketCenterId) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (SubMarketCenterId × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : SubMarketCenterId) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : SubMarketCenterId) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end SubMarketCenterId

/-- Issue Type: one byte code -/
def IssueType.codes : List UInt8 :=
  [0x41, 0x42, 0x43, 0x46, 0x49, 0x4C, 0x4E, 0x4F, 0x50, 0x51, 0x52, 0x53, 0x54, 0x55, 0x56, 0x57]

inductive IssueType where
  | americanDepositoryReceipt -- American Depository Receipt
  | bond -- Bond
  | commonStock -- Common Stock
  | depositoryReceipt -- Depository Receipt
  | sec144A -- Sec 144 A
  | limitedPartnership -- Limited Partnership
  | note -- Note
  | ordinaryShares -- Ordinary Shares
  | preferredStock -- Preferred Stock
  | otherSecurities -- Other Securities
  | rights -- Rights
  | sharesOfBeneficialInterest -- Shares Of Beneficial Interest
  | convertibleDebenture -- Convertible Debenture
  | unit -- Unit
  | unitsOfBeneficialInterest -- Units Of Beneficial Interest
  | warrant -- Warrant
  | unlisted (byte : { byte : UInt8 // byte ∉ IssueType.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace IssueType

def toByte : IssueType → UInt8
  | .americanDepositoryReceipt => 0x41
  | .bond => 0x42
  | .commonStock => 0x43
  | .depositoryReceipt => 0x46
  | .sec144A => 0x49
  | .limitedPartnership => 0x4C
  | .note => 0x4E
  | .ordinaryShares => 0x4F
  | .preferredStock => 0x50
  | .otherSecurities => 0x51
  | .rights => 0x52
  | .sharesOfBeneficialInterest => 0x53
  | .convertibleDebenture => 0x54
  | .unit => 0x55
  | .unitsOfBeneficialInterest => 0x56
  | .warrant => 0x57
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : IssueType :=
  if byte = 0x41 then .americanDepositoryReceipt
  else if byte = 0x42 then .bond
  else if byte = 0x43 then .commonStock
  else if byte = 0x46 then .depositoryReceipt
  else if byte = 0x49 then .sec144A
  else if byte = 0x4C then .limitedPartnership
  else if byte = 0x4E then .note
  else if byte = 0x4F then .ordinaryShares
  else if byte = 0x50 then .preferredStock
  else if byte = 0x51 then .otherSecurities
  else if byte = 0x52 then .rights
  else if byte = 0x53 then .sharesOfBeneficialInterest
  else if byte = 0x54 then .convertibleDebenture
  else if byte = 0x55 then .unit
  else if byte = 0x56 then .unitsOfBeneficialInterest
  else .warrant

def ofByte (byte : UInt8) : IssueType :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : IssueType) : ofByte value.toByte = value := by
  cases value with
  | americanDepositoryReceipt => decide
  | bond => decide
  | commonStock => decide
  | depositoryReceipt => decide
  | sec144A => decide
  | limitedPartnership => decide
  | note => decide
  | ordinaryShares => decide
  | preferredStock => decide
  | otherSecurities => decide
  | rights => decide
  | sharesOfBeneficialInterest => decide
  | convertibleDebenture => decide
  | unit => decide
  | unitsOfBeneficialInterest => decide
  | warrant => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : IssueType) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (IssueType × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : IssueType) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : IssueType) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end IssueType

/-- Market Tier: one byte code -/
def MarketTier.codes : List UInt8 :=
  [0x51, 0x47, 0x53]

inductive MarketTier where
  | nasdaqGlobalSelectMarket -- Nasdaq Global Select Market
  | nasdaqGlobalMarket -- Nasdaq Global Market
  | nasdaqCapitalMarket -- Nasdaq Capital Market
  | unlisted (byte : { byte : UInt8 // byte ∉ MarketTier.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace MarketTier

def toByte : MarketTier → UInt8
  | .nasdaqGlobalSelectMarket => 0x51
  | .nasdaqGlobalMarket => 0x47
  | .nasdaqCapitalMarket => 0x53
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : MarketTier :=
  if byte = 0x51 then .nasdaqGlobalSelectMarket
  else if byte = 0x47 then .nasdaqGlobalMarket
  else .nasdaqCapitalMarket

def ofByte (byte : UInt8) : MarketTier :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : MarketTier) : ofByte value.toByte = value := by
  cases value with
  | nasdaqGlobalSelectMarket => decide
  | nasdaqGlobalMarket => decide
  | nasdaqCapitalMarket => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : MarketTier) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (MarketTier × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : MarketTier) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : MarketTier) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end MarketTier

/-- Authenticity: one byte code -/
def Authenticity.codes : List UInt8 :=
  [0x50, 0x54, 0x44, 0x58]

inductive Authenticity where
  | liveProduction -- Live Production
  | test -- Test
  | demo -- Demo
  | deleted -- Deleted
  | unlisted (byte : { byte : UInt8 // byte ∉ Authenticity.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace Authenticity

def toByte : Authenticity → UInt8
  | .liveProduction => 0x50
  | .test => 0x54
  | .demo => 0x44
  | .deleted => 0x58
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : Authenticity :=
  if byte = 0x50 then .liveProduction
  else if byte = 0x54 then .test
  else if byte = 0x44 then .demo
  else .deleted

def ofByte (byte : UInt8) : Authenticity :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : Authenticity) : ofByte value.toByte = value := by
  cases value with
  | liveProduction => decide
  | test => decide
  | demo => decide
  | deleted => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : Authenticity) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (Authenticity × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : Authenticity) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : Authenticity) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end Authenticity

/-- Short Sale Threshold Indicator: one byte code -/
def ShortSaleThresholdIndicator.codes : List UInt8 :=
  [0x59, 0x4E, 0x20]

inductive ShortSaleThresholdIndicator where
  | issueIsRestrictedUnderSecRule203B3 -- Issue Is Restricted Under Sec Rule 203 B 3
  | issueIsNotRestricted -- Issue Is Not Restricted
  | thresholdIndicatorNotAvailable -- Threshold Indicator Not Available
  | unlisted (byte : { byte : UInt8 // byte ∉ ShortSaleThresholdIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace ShortSaleThresholdIndicator

def toByte : ShortSaleThresholdIndicator → UInt8
  | .issueIsRestrictedUnderSecRule203B3 => 0x59
  | .issueIsNotRestricted => 0x4E
  | .thresholdIndicatorNotAvailable => 0x20
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : ShortSaleThresholdIndicator :=
  if byte = 0x59 then .issueIsRestrictedUnderSecRule203B3
  else if byte = 0x4E then .issueIsNotRestricted
  else .thresholdIndicatorNotAvailable

def ofByte (byte : UInt8) : ShortSaleThresholdIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : ShortSaleThresholdIndicator) : ofByte value.toByte = value := by
  cases value with
  | issueIsRestrictedUnderSecRule203B3 => decide
  | issueIsNotRestricted => decide
  | thresholdIndicatorNotAvailable => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : ShortSaleThresholdIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (ShortSaleThresholdIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : ShortSaleThresholdIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : ShortSaleThresholdIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end ShortSaleThresholdIndicator

/-- Financial Status Indicator: one byte code -/
def FinancialStatusIndicator.codes : List UInt8 :=
  [0x43, 0x44, 0x45, 0x51, 0x4E, 0x47, 0x48, 0x4A, 0x4B]

inductive FinancialStatusIndicator where
  | creationsAndorRedemptionsSuspendedForExchangeTradedProduct -- Creations Andor Redemptions Suspended For Exchange Traded Product
  | deficientIssuerFailedToMeetNasdaqContinuedListingRequirements -- Deficient Issuer Failed To Meet Nasdaq Continued Listing Requirements
  | delinquentIssuerMissedRegulatoryFilingDeadline -- Delinquent Issuer Missed Regulatory Filing Deadline
  | bankruptIssuerHasFiledForBankruptcy -- Bankrupt Issuer Has Filed For Bankruptcy
  | normalDefaultIssuerIsNotDeficientDelinquentOrBankrupt -- Normal Default Issuer Is Not Deficient Delinquent Or Bankrupt
  | deficientAndBankrupt -- Deficient And Bankrupt
  | deficientAndDelinquent -- Deficient And Delinquent
  | delinquentAndBankrupt -- Delinquent And Bankrupt
  | deficientDelinquentAndBankrupt -- Deficient Delinquent And Bankrupt
  | unlisted (byte : { byte : UInt8 // byte ∉ FinancialStatusIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace FinancialStatusIndicator

def toByte : FinancialStatusIndicator → UInt8
  | .creationsAndorRedemptionsSuspendedForExchangeTradedProduct => 0x43
  | .deficientIssuerFailedToMeetNasdaqContinuedListingRequirements => 0x44
  | .delinquentIssuerMissedRegulatoryFilingDeadline => 0x45
  | .bankruptIssuerHasFiledForBankruptcy => 0x51
  | .normalDefaultIssuerIsNotDeficientDelinquentOrBankrupt => 0x4E
  | .deficientAndBankrupt => 0x47
  | .deficientAndDelinquent => 0x48
  | .delinquentAndBankrupt => 0x4A
  | .deficientDelinquentAndBankrupt => 0x4B
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : FinancialStatusIndicator :=
  if byte = 0x43 then .creationsAndorRedemptionsSuspendedForExchangeTradedProduct
  else if byte = 0x44 then .deficientIssuerFailedToMeetNasdaqContinuedListingRequirements
  else if byte = 0x45 then .delinquentIssuerMissedRegulatoryFilingDeadline
  else if byte = 0x51 then .bankruptIssuerHasFiledForBankruptcy
  else if byte = 0x4E then .normalDefaultIssuerIsNotDeficientDelinquentOrBankrupt
  else if byte = 0x47 then .deficientAndBankrupt
  else if byte = 0x48 then .deficientAndDelinquent
  else if byte = 0x4A then .delinquentAndBankrupt
  else .deficientDelinquentAndBankrupt

def ofByte (byte : UInt8) : FinancialStatusIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : FinancialStatusIndicator) : ofByte value.toByte = value := by
  cases value with
  | creationsAndorRedemptionsSuspendedForExchangeTradedProduct => decide
  | deficientIssuerFailedToMeetNasdaqContinuedListingRequirements => decide
  | delinquentIssuerMissedRegulatoryFilingDeadline => decide
  | bankruptIssuerHasFiledForBankruptcy => decide
  | normalDefaultIssuerIsNotDeficientDelinquentOrBankrupt => decide
  | deficientAndBankrupt => decide
  | deficientAndDelinquent => decide
  | delinquentAndBankrupt => decide
  | deficientDelinquentAndBankrupt => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : FinancialStatusIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (FinancialStatusIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : FinancialStatusIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : FinancialStatusIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end FinancialStatusIndicator

/-- Mpi Tick: one byte code -/
def MpiTick.codes : List UInt8 :=
  [0x50, 0x48]

inductive MpiTick where
  | pennyMpi -- Penny Mpi
  | halfPennyMpiEffectiveNov2026PerSecMandate -- Half Penny Mpi Effective Nov 2026 Per Sec Mandate
  | unlisted (byte : { byte : UInt8 // byte ∉ MpiTick.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace MpiTick

def toByte : MpiTick → UInt8
  | .pennyMpi => 0x50
  | .halfPennyMpiEffectiveNov2026PerSecMandate => 0x48
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : MpiTick :=
  if byte = 0x50 then .pennyMpi
  else .halfPennyMpiEffectiveNov2026PerSecMandate

def ofByte (byte : UInt8) : MpiTick :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : MpiTick) : ofByte value.toByte = value := by
  cases value with
  | pennyMpi => decide
  | halfPennyMpiEffectiveNov2026PerSecMandate => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : MpiTick) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (MpiTick × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : MpiTick) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : MpiTick) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end MpiTick

/-- Trading State: one byte code -/
def TradingState.codes : List UInt8 :=
  [0x48, 0x54]

inductive TradingState where
  | halted -- Halted
  | trading -- Trading
  | unlisted (byte : { byte : UInt8 // byte ∉ TradingState.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace TradingState

def toByte : TradingState → UInt8
  | .halted => 0x48
  | .trading => 0x54
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : TradingState :=
  if byte = 0x48 then .halted
  else .trading

def ofByte (byte : UInt8) : TradingState :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : TradingState) : ofByte value.toByte = value := by
  cases value with
  | halted => decide
  | trading => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : TradingState) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (TradingState × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : TradingState) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : TradingState) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end TradingState

/-- Reg Sho Action: one byte code -/
def RegShoAction.codes : List UInt8 :=
  [0x30, 0x31, 0x32]

inductive RegShoAction where
  | noPriceTestInEffect -- No Price Test In Effect
  | regShoInEffectDueToAnIntraDayPriceDropInSecurity -- Reg Sho In Effect Due To An Intra Day Price Drop In Security
  | regShoRestrictionRemainsInEffect -- Reg Sho Restriction Remains In Effect
  | unlisted (byte : { byte : UInt8 // byte ∉ RegShoAction.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace RegShoAction

def toByte : RegShoAction → UInt8
  | .noPriceTestInEffect => 0x30
  | .regShoInEffectDueToAnIntraDayPriceDropInSecurity => 0x31
  | .regShoRestrictionRemainsInEffect => 0x32
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : RegShoAction :=
  if byte = 0x30 then .noPriceTestInEffect
  else if byte = 0x31 then .regShoInEffectDueToAnIntraDayPriceDropInSecurity
  else .regShoRestrictionRemainsInEffect

def ofByte (byte : UInt8) : RegShoAction :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : RegShoAction) : ofByte value.toByte = value := by
  cases value with
  | noPriceTestInEffect => decide
  | regShoInEffectDueToAnIntraDayPriceDropInSecurity => decide
  | regShoRestrictionRemainsInEffect => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : RegShoAction) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (RegShoAction × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : RegShoAction) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : RegShoAction) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end RegShoAction

/-- Trading Action Code: one byte code -/
def TradingActionCode.codes : List UInt8 :=
  [0x48, 0x51, 0x54, 0x50]

inductive TradingActionCode where
  | tradingHalt -- Trading Halt
  | quotationResumptionIncludingAfterEma -- Quotation Resumption Including After Ema
  | tradingResumption -- Trading Resumption
  | volatilityTradingPause -- Volatility Trading Pause
  | unlisted (byte : { byte : UInt8 // byte ∉ TradingActionCode.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace TradingActionCode

def toByte : TradingActionCode → UInt8
  | .tradingHalt => 0x48
  | .quotationResumptionIncludingAfterEma => 0x51
  | .tradingResumption => 0x54
  | .volatilityTradingPause => 0x50
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : TradingActionCode :=
  if byte = 0x48 then .tradingHalt
  else if byte = 0x51 then .quotationResumptionIncludingAfterEma
  else if byte = 0x54 then .tradingResumption
  else .volatilityTradingPause

def ofByte (byte : UInt8) : TradingActionCode :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : TradingActionCode) : ofByte value.toByte = value := by
  cases value with
  | tradingHalt => decide
  | quotationResumptionIncludingAfterEma => decide
  | tradingResumption => decide
  | volatilityTradingPause => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : TradingActionCode) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (TradingActionCode × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : TradingActionCode) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : TradingActionCode) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end TradingActionCode

/-- Luld Price Band Indicator: one byte code -/
def LuldPriceBandIndicator.codes : List UInt8 :=
  [0x41, 0x42, 0x43, 0x44, 0x45, 0x46, 0x20]

inductive LuldPriceBandIndicator where
  | openingUpdate -- Opening Update
  | intraDayUpdate -- Intra Day Update
  | restatedValue -- Restated Value
  | suspendedDuringTradingHaltOrTradingPause -- Suspended During Trading Halt Or Trading Pause
  | reOpeningUpdate -- Re Opening Update
  | outsidePriceBandRuleHours -- Outside Price Band Rule Hours
  | noneProvided -- None Provided
  | unlisted (byte : { byte : UInt8 // byte ∉ LuldPriceBandIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace LuldPriceBandIndicator

def toByte : LuldPriceBandIndicator → UInt8
  | .openingUpdate => 0x41
  | .intraDayUpdate => 0x42
  | .restatedValue => 0x43
  | .suspendedDuringTradingHaltOrTradingPause => 0x44
  | .reOpeningUpdate => 0x45
  | .outsidePriceBandRuleHours => 0x46
  | .noneProvided => 0x20
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : LuldPriceBandIndicator :=
  if byte = 0x41 then .openingUpdate
  else if byte = 0x42 then .intraDayUpdate
  else if byte = 0x43 then .restatedValue
  else if byte = 0x44 then .suspendedDuringTradingHaltOrTradingPause
  else if byte = 0x45 then .reOpeningUpdate
  else if byte = 0x46 then .outsidePriceBandRuleHours
  else .noneProvided

def ofByte (byte : UInt8) : LuldPriceBandIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : LuldPriceBandIndicator) : ofByte value.toByte = value := by
  cases value with
  | openingUpdate => decide
  | intraDayUpdate => decide
  | restatedValue => decide
  | suspendedDuringTradingHaltOrTradingPause => decide
  | reOpeningUpdate => decide
  | outsidePriceBandRuleHours => decide
  | noneProvided => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : LuldPriceBandIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (LuldPriceBandIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : LuldPriceBandIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : LuldPriceBandIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end LuldPriceBandIndicator

/-- Quote Condition: one byte code -/
def QuoteCondition.codes : List UInt8 :=
  [0x41, 0x42, 0x46, 0x48, 0x49, 0x4C, 0x4E, 0x4F, 0x52, 0x55, 0x58, 0x59, 0x5A, 0x34]

inductive QuoteCondition where
  | manualAskAutomatedBid -- Manual Ask Automated Bid
  | manualBidAutomatedAsk -- Manual Bid Automated Ask
  | fastTrading -- Fast Trading
  | manualBidAndAsk -- Manual Bid And Ask
  | orderImbalance -- Order Imbalance
  | closedQuote -- Closed Quote
  | nonfirmQuote -- Nonfirm Quote
  | openingQuoteAutomated -- Opening Quote Automated
  | regularTwosidedOpenQuoteAutomated -- Regular Twosided Open Quote Automated
  | manualBidAndAskNonfirm -- Manual Bid And Ask Nonfirm
  | orderInflux -- Order Influx
  | automatedBidNoOfferOrAutomatedOfferNoBid -- Automated Bid No Offer Or Automated Offer No Bid
  | noOpenNoResume -- No Open No Resume
  | intradayAuction -- Intraday Auction
  | unlisted (byte : { byte : UInt8 // byte ∉ QuoteCondition.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace QuoteCondition

def toByte : QuoteCondition → UInt8
  | .manualAskAutomatedBid => 0x41
  | .manualBidAutomatedAsk => 0x42
  | .fastTrading => 0x46
  | .manualBidAndAsk => 0x48
  | .orderImbalance => 0x49
  | .closedQuote => 0x4C
  | .nonfirmQuote => 0x4E
  | .openingQuoteAutomated => 0x4F
  | .regularTwosidedOpenQuoteAutomated => 0x52
  | .manualBidAndAskNonfirm => 0x55
  | .orderInflux => 0x58
  | .automatedBidNoOfferOrAutomatedOfferNoBid => 0x59
  | .noOpenNoResume => 0x5A
  | .intradayAuction => 0x34
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : QuoteCondition :=
  if byte = 0x41 then .manualAskAutomatedBid
  else if byte = 0x42 then .manualBidAutomatedAsk
  else if byte = 0x46 then .fastTrading
  else if byte = 0x48 then .manualBidAndAsk
  else if byte = 0x49 then .orderImbalance
  else if byte = 0x4C then .closedQuote
  else if byte = 0x4E then .nonfirmQuote
  else if byte = 0x4F then .openingQuoteAutomated
  else if byte = 0x52 then .regularTwosidedOpenQuoteAutomated
  else if byte = 0x55 then .manualBidAndAskNonfirm
  else if byte = 0x58 then .orderInflux
  else if byte = 0x59 then .automatedBidNoOfferOrAutomatedOfferNoBid
  else if byte = 0x5A then .noOpenNoResume
  else .intradayAuction

def ofByte (byte : UInt8) : QuoteCondition :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : QuoteCondition) : ofByte value.toByte = value := by
  cases value with
  | manualAskAutomatedBid => decide
  | manualBidAutomatedAsk => decide
  | fastTrading => decide
  | manualBidAndAsk => decide
  | orderImbalance => decide
  | closedQuote => decide
  | nonfirmQuote => decide
  | openingQuoteAutomated => decide
  | regularTwosidedOpenQuoteAutomated => decide
  | manualBidAndAskNonfirm => decide
  | orderInflux => decide
  | automatedBidNoOfferOrAutomatedOfferNoBid => decide
  | noOpenNoResume => decide
  | intradayAuction => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : QuoteCondition) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (QuoteCondition × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : QuoteCondition) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : QuoteCondition) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end QuoteCondition

/-- Sip Generated Update Flag: one byte code -/
def SipGeneratedUpdateFlag.codes : List UInt8 :=
  [0x20, 0x45]

inductive SipGeneratedUpdateFlag where
  | transactionOriginatedFromMarketParticipant -- Transaction Originated From Market Participant
  | siPgeneratedTransactionEgPriceBandChange -- Si Pgenerated Transaction Eg Price Band Change
  | unlisted (byte : { byte : UInt8 // byte ∉ SipGeneratedUpdateFlag.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace SipGeneratedUpdateFlag

def toByte : SipGeneratedUpdateFlag → UInt8
  | .transactionOriginatedFromMarketParticipant => 0x20
  | .siPgeneratedTransactionEgPriceBandChange => 0x45
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : SipGeneratedUpdateFlag :=
  if byte = 0x20 then .transactionOriginatedFromMarketParticipant
  else .siPgeneratedTransactionEgPriceBandChange

def ofByte (byte : UInt8) : SipGeneratedUpdateFlag :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : SipGeneratedUpdateFlag) : ofByte value.toByte = value := by
  cases value with
  | transactionOriginatedFromMarketParticipant => decide
  | siPgeneratedTransactionEgPriceBandChange => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : SipGeneratedUpdateFlag) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (SipGeneratedUpdateFlag × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : SipGeneratedUpdateFlag) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : SipGeneratedUpdateFlag) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end SipGeneratedUpdateFlag

/-- Luld Bbo Indicator: one byte code -/
def LuldBboIndicator.codes : List UInt8 :=
  [0x20, 0x41, 0x42, 0x43]

inductive LuldBboIndicator where
  | limitUpLimitDownNotApplicable -- Limit Up Limit Down Not Applicable
  | bidPriceAboveUpperLimitPriceBandBidIsNonExecutable -- Bid Price Above Upper Limit Price Band Bid Is Non Executable
  | askPriceBelowLowerLimitPriceBandAskIsNonExecutable -- Ask Price Below Lower Limit Price Band Ask Is Non Executable
  | bidAndAskOutsidePriceBandNotExecutable -- Bid And Ask Outside Price Band Not Executable
  | unlisted (byte : { byte : UInt8 // byte ∉ LuldBboIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace LuldBboIndicator

def toByte : LuldBboIndicator → UInt8
  | .limitUpLimitDownNotApplicable => 0x20
  | .bidPriceAboveUpperLimitPriceBandBidIsNonExecutable => 0x41
  | .askPriceBelowLowerLimitPriceBandAskIsNonExecutable => 0x42
  | .bidAndAskOutsidePriceBandNotExecutable => 0x43
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : LuldBboIndicator :=
  if byte = 0x20 then .limitUpLimitDownNotApplicable
  else if byte = 0x41 then .bidPriceAboveUpperLimitPriceBandBidIsNonExecutable
  else if byte = 0x42 then .askPriceBelowLowerLimitPriceBandAskIsNonExecutable
  else .bidAndAskOutsidePriceBandNotExecutable

def ofByte (byte : UInt8) : LuldBboIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : LuldBboIndicator) : ofByte value.toByte = value := by
  cases value with
  | limitUpLimitDownNotApplicable => decide
  | bidPriceAboveUpperLimitPriceBandBidIsNonExecutable => decide
  | askPriceBelowLowerLimitPriceBandAskIsNonExecutable => decide
  | bidAndAskOutsidePriceBandNotExecutable => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : LuldBboIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (LuldBboIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : LuldBboIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : LuldBboIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end LuldBboIndicator

/-- Retail Interest Indicator: one byte code -/
def RetailInterestIndicator.codes : List UInt8 :=
  [0x20, 0x41, 0x42, 0x43]

inductive RetailInterestIndicator where
  | retailInterestNotApplicable -- Retail Interest Not Applicable
  | retailInterestOnBidQuote -- Retail Interest On Bid Quote
  | retailInterestOnAskQuote -- Retail Interest On Ask Quote
  | retailInterestOnBothBidAndAskQuote -- Retail Interest On Both Bid And Ask Quote
  | unlisted (byte : { byte : UInt8 // byte ∉ RetailInterestIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace RetailInterestIndicator

def toByte : RetailInterestIndicator → UInt8
  | .retailInterestNotApplicable => 0x20
  | .retailInterestOnBidQuote => 0x41
  | .retailInterestOnAskQuote => 0x42
  | .retailInterestOnBothBidAndAskQuote => 0x43
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : RetailInterestIndicator :=
  if byte = 0x20 then .retailInterestNotApplicable
  else if byte = 0x41 then .retailInterestOnBidQuote
  else if byte = 0x42 then .retailInterestOnAskQuote
  else .retailInterestOnBothBidAndAskQuote

def ofByte (byte : UInt8) : RetailInterestIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : RetailInterestIndicator) : ofByte value.toByte = value := by
  cases value with
  | retailInterestNotApplicable => decide
  | retailInterestOnBidQuote => decide
  | retailInterestOnAskQuote => decide
  | retailInterestOnBothBidAndAskQuote => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : RetailInterestIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (RetailInterestIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : RetailInterestIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : RetailInterestIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end RetailInterestIndicator

/-- Nbbo Appendage Indicator: one byte code -/
def NbboAppendageIndicator.codes : List UInt8 :=
  [0x30, 0x31, 0x32, 0x33, 0x34]

inductive NbboAppendageIndicator where
  | noNationalBboChange -- No National Bbo Change
  | noNationalBboCanBeCalculated -- No National Bbo Can Be Calculated
  | shortFormNationalBboAppendageAttached -- Short Form National Bbo Appendage Attached
  | longFormNationalBboAppendageAttached -- Long Form National Bbo Appendage Attached
  | quoteContainsAllNationalBboInformation -- Quote Contains All National Bbo Information
  | unlisted (byte : { byte : UInt8 // byte ∉ NbboAppendageIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace NbboAppendageIndicator

def toByte : NbboAppendageIndicator → UInt8
  | .noNationalBboChange => 0x30
  | .noNationalBboCanBeCalculated => 0x31
  | .shortFormNationalBboAppendageAttached => 0x32
  | .longFormNationalBboAppendageAttached => 0x33
  | .quoteContainsAllNationalBboInformation => 0x34
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : NbboAppendageIndicator :=
  if byte = 0x30 then .noNationalBboChange
  else if byte = 0x31 then .noNationalBboCanBeCalculated
  else if byte = 0x32 then .shortFormNationalBboAppendageAttached
  else if byte = 0x33 then .longFormNationalBboAppendageAttached
  else .quoteContainsAllNationalBboInformation

def ofByte (byte : UInt8) : NbboAppendageIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : NbboAppendageIndicator) : ofByte value.toByte = value := by
  cases value with
  | noNationalBboChange => decide
  | noNationalBboCanBeCalculated => decide
  | shortFormNationalBboAppendageAttached => decide
  | longFormNationalBboAppendageAttached => decide
  | quoteContainsAllNationalBboInformation => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : NbboAppendageIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (NbboAppendageIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : NbboAppendageIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : NbboAppendageIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end NbboAppendageIndicator

/-- Luld National Bbo Indicator: one byte code -/
def LuldNationalBboIndicator.codes : List UInt8 :=
  [0x20, 0x41, 0x42, 0x43, 0x44, 0x45, 0x46, 0x47, 0x48, 0x49]

inductive LuldNationalBboIndicator where
  | limitUpLimitDownNotApplicable -- Limit Up Limit Down Not Applicable
  | nationalBestBidAndNationalBestAskAreExecutable -- National Best Bid And National Best Ask Are Executable
  | nationalBestBidBelowLowerLimitPriceBandNonExecutable -- National Best Bid Below Lower Limit Price Band Non Executable
  | nationalBestAskAboveUpperLimitPriceBandNonExecutable -- National Best Ask Above Upper Limit Price Band Non Executable
  | nationalBestBidBelowLowerAndNationalBestAskAboveUpperLimitPriceBandsNonExecutable -- National Best Bid Below Lower And National Best Ask Above Upper Limit Price Bands Non Executable
  | nationalBestBidEqualsUpperLimitPriceBandNationalBestBidIsInLimitState -- National Best Bid Equals Upper Limit Price Band National Best Bid Is In Limit State
  | nationalBestOfferEqualsLowerLimitPriceBandNationalBestAskIsInLimitState -- National Best Offer Equals Lower Limit Price Band National Best Ask Is In Limit State
  | nationalBestBidEqualsUpperLimitPriceBandInLimitStateAndNationalBestAskAboveUpperLimitPriceBandNonExecutable -- National Best Bid Equals Upper Limit Price Band In Limit State And National Best Ask Above Upper Limit Price Band Non Executable
  | nationalBestAskEqualsLowerLimitPriceBandInLimitStateAndNationalBestBidBelowLowerLimitPriceBandNonExecutable -- National Best Ask Equals Lower Limit Price Band In Limit State And National Best Bid Below Lower Limit Price Band Non Executable
  | nationalBestBidEqualsUpperLimitPriceBandAndNationalBestAskEqualsLowerLimitPriceBandCrossedNotInLimitState -- National Best Bid Equals Upper Limit Price Band And National Best Ask Equals Lower Limit Price Band Crossed Not In Limit State
  | unlisted (byte : { byte : UInt8 // byte ∉ LuldNationalBboIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace LuldNationalBboIndicator

def toByte : LuldNationalBboIndicator → UInt8
  | .limitUpLimitDownNotApplicable => 0x20
  | .nationalBestBidAndNationalBestAskAreExecutable => 0x41
  | .nationalBestBidBelowLowerLimitPriceBandNonExecutable => 0x42
  | .nationalBestAskAboveUpperLimitPriceBandNonExecutable => 0x43
  | .nationalBestBidBelowLowerAndNationalBestAskAboveUpperLimitPriceBandsNonExecutable => 0x44
  | .nationalBestBidEqualsUpperLimitPriceBandNationalBestBidIsInLimitState => 0x45
  | .nationalBestOfferEqualsLowerLimitPriceBandNationalBestAskIsInLimitState => 0x46
  | .nationalBestBidEqualsUpperLimitPriceBandInLimitStateAndNationalBestAskAboveUpperLimitPriceBandNonExecutable => 0x47
  | .nationalBestAskEqualsLowerLimitPriceBandInLimitStateAndNationalBestBidBelowLowerLimitPriceBandNonExecutable => 0x48
  | .nationalBestBidEqualsUpperLimitPriceBandAndNationalBestAskEqualsLowerLimitPriceBandCrossedNotInLimitState => 0x49
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : LuldNationalBboIndicator :=
  if byte = 0x20 then .limitUpLimitDownNotApplicable
  else if byte = 0x41 then .nationalBestBidAndNationalBestAskAreExecutable
  else if byte = 0x42 then .nationalBestBidBelowLowerLimitPriceBandNonExecutable
  else if byte = 0x43 then .nationalBestAskAboveUpperLimitPriceBandNonExecutable
  else if byte = 0x44 then .nationalBestBidBelowLowerAndNationalBestAskAboveUpperLimitPriceBandsNonExecutable
  else if byte = 0x45 then .nationalBestBidEqualsUpperLimitPriceBandNationalBestBidIsInLimitState
  else if byte = 0x46 then .nationalBestOfferEqualsLowerLimitPriceBandNationalBestAskIsInLimitState
  else if byte = 0x47 then .nationalBestBidEqualsUpperLimitPriceBandInLimitStateAndNationalBestAskAboveUpperLimitPriceBandNonExecutable
  else if byte = 0x48 then .nationalBestAskEqualsLowerLimitPriceBandInLimitStateAndNationalBestBidBelowLowerLimitPriceBandNonExecutable
  else .nationalBestBidEqualsUpperLimitPriceBandAndNationalBestAskEqualsLowerLimitPriceBandCrossedNotInLimitState

def ofByte (byte : UInt8) : LuldNationalBboIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : LuldNationalBboIndicator) : ofByte value.toByte = value := by
  cases value with
  | limitUpLimitDownNotApplicable => decide
  | nationalBestBidAndNationalBestAskAreExecutable => decide
  | nationalBestBidBelowLowerLimitPriceBandNonExecutable => decide
  | nationalBestAskAboveUpperLimitPriceBandNonExecutable => decide
  | nationalBestBidBelowLowerAndNationalBestAskAboveUpperLimitPriceBandsNonExecutable => decide
  | nationalBestBidEqualsUpperLimitPriceBandNationalBestBidIsInLimitState => decide
  | nationalBestOfferEqualsLowerLimitPriceBandNationalBestAskIsInLimitState => decide
  | nationalBestBidEqualsUpperLimitPriceBandInLimitStateAndNationalBestAskAboveUpperLimitPriceBandNonExecutable => decide
  | nationalBestAskEqualsLowerLimitPriceBandInLimitStateAndNationalBestBidBelowLowerLimitPriceBandNonExecutable => decide
  | nationalBestBidEqualsUpperLimitPriceBandAndNationalBestAskEqualsLowerLimitPriceBandCrossedNotInLimitState => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : LuldNationalBboIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (LuldNationalBboIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : LuldNationalBboIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : LuldNationalBboIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end LuldNationalBboIndicator

/-- Finra Adf Mpid Appendage Indicator: one byte code -/
def FinraAdfMpidAppendageIndicator.codes : List UInt8 :=
  [0x20, 0x30, 0x31, 0x32]

inductive FinraAdfMpidAppendageIndicator where
  | notApplicableQuotationOriginatesFromAUtpParticipantOtherThanFinra -- Not Applicable Quotation Originates From A Utp Participant Other Than Finra
  | noAdfMpidChangesCurrentAdfQuoteDoesNotAffectTheFinraMpid -- No Adf Mpid Changes Current Adf Quote Does Not Affect The Finra Mpid
  | noAdfMpidExistsNoCalculationOfTheFinraMpid -- No Adf Mpid Exists No Calculation Of The Finra Mpid
  | adfMpiDsAttachedFinraMpidWasGenerated -- Adf Mpi Ds Attached Finra Mpid Was Generated
  | unlisted (byte : { byte : UInt8 // byte ∉ FinraAdfMpidAppendageIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace FinraAdfMpidAppendageIndicator

def toByte : FinraAdfMpidAppendageIndicator → UInt8
  | .notApplicableQuotationOriginatesFromAUtpParticipantOtherThanFinra => 0x20
  | .noAdfMpidChangesCurrentAdfQuoteDoesNotAffectTheFinraMpid => 0x30
  | .noAdfMpidExistsNoCalculationOfTheFinraMpid => 0x31
  | .adfMpiDsAttachedFinraMpidWasGenerated => 0x32
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : FinraAdfMpidAppendageIndicator :=
  if byte = 0x20 then .notApplicableQuotationOriginatesFromAUtpParticipantOtherThanFinra
  else if byte = 0x30 then .noAdfMpidChangesCurrentAdfQuoteDoesNotAffectTheFinraMpid
  else if byte = 0x31 then .noAdfMpidExistsNoCalculationOfTheFinraMpid
  else .adfMpiDsAttachedFinraMpidWasGenerated

def ofByte (byte : UInt8) : FinraAdfMpidAppendageIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : FinraAdfMpidAppendageIndicator) : ofByte value.toByte = value := by
  cases value with
  | notApplicableQuotationOriginatesFromAUtpParticipantOtherThanFinra => decide
  | noAdfMpidChangesCurrentAdfQuoteDoesNotAffectTheFinraMpid => decide
  | noAdfMpidExistsNoCalculationOfTheFinraMpid => decide
  | adfMpiDsAttachedFinraMpidWasGenerated => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : FinraAdfMpidAppendageIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (FinraAdfMpidAppendageIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : FinraAdfMpidAppendageIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : FinraAdfMpidAppendageIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end FinraAdfMpidAppendageIndicator

/-- Bolo Appendage Indicator: one byte code -/
def BoloAppendageIndicator.codes : List UInt8 :=
  [0x30, 0x31, 0x32, 0x33, 0x35]

inductive BoloAppendageIndicator where
  | noBoloChange -- No Bolo Change
  | noBoloCanBeCalculated -- No Bolo Can Be Calculated
  | shortFormBoloAppendageAttached -- Short Form Bolo Appendage Attached
  | longFormBoloAppendageAttached -- Long Form Bolo Appendage Attached
  | mpidFormBoloAppendageAttached -- Mpid Form Bolo Appendage Attached
  | unlisted (byte : { byte : UInt8 // byte ∉ BoloAppendageIndicator.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace BoloAppendageIndicator

def toByte : BoloAppendageIndicator → UInt8
  | .noBoloChange => 0x30
  | .noBoloCanBeCalculated => 0x31
  | .shortFormBoloAppendageAttached => 0x32
  | .longFormBoloAppendageAttached => 0x33
  | .mpidFormBoloAppendageAttached => 0x35
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : BoloAppendageIndicator :=
  if byte = 0x30 then .noBoloChange
  else if byte = 0x31 then .noBoloCanBeCalculated
  else if byte = 0x32 then .shortFormBoloAppendageAttached
  else if byte = 0x33 then .longFormBoloAppendageAttached
  else .mpidFormBoloAppendageAttached

def ofByte (byte : UInt8) : BoloAppendageIndicator :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : BoloAppendageIndicator) : ofByte value.toByte = value := by
  cases value with
  | noBoloChange => decide
  | noBoloCanBeCalculated => decide
  | shortFormBoloAppendageAttached => decide
  | longFormBoloAppendageAttached => decide
  | mpidFormBoloAppendageAttached => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : BoloAppendageIndicator) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (BoloAppendageIndicator × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : BoloAppendageIndicator) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : BoloAppendageIndicator) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end BoloAppendageIndicator

/-- Odd Lot Attachment Type: one byte code -/
def OddLotAttachmentType.codes : List UInt8 :=
  [0x30, 0x32, 0x33, 0x35]

inductive OddLotAttachmentType where
  | noOddLotPriceLevelsAttached -- No Odd Lot Price Levels Attached
  | shortFormOddLotPriceLevelsAttached -- Short Form Odd Lot Price Levels Attached
  | longFormOddLotPriceLevelsAttached -- Long Form Odd Lot Price Levels Attached
  | mpidFormOddLotPriceLevelsAttached -- Mpid Form Odd Lot Price Levels Attached
  | unlisted (byte : { byte : UInt8 // byte ∉ OddLotAttachmentType.codes }) -- any other code, kept as it is
  deriving DecidableEq, Repr

namespace OddLotAttachmentType

def toByte : OddLotAttachmentType → UInt8
  | .noOddLotPriceLevelsAttached => 0x30
  | .shortFormOddLotPriceLevelsAttached => 0x32
  | .longFormOddLotPriceLevelsAttached => 0x33
  | .mpidFormOddLotPriceLevelsAttached => 0x35
  | .unlisted byte => byte.val

/-- The constructor of a listed code -/
def listed (byte : UInt8) : OddLotAttachmentType :=
  if byte = 0x30 then .noOddLotPriceLevelsAttached
  else if byte = 0x32 then .shortFormOddLotPriceLevelsAttached
  else if byte = 0x33 then .longFormOddLotPriceLevelsAttached
  else .mpidFormOddLotPriceLevelsAttached

def ofByte (byte : UInt8) : OddLotAttachmentType :=
  if known : byte ∈ codes then listed byte else .unlisted ⟨byte, known⟩

theorem ofByte_toByte (value : OddLotAttachmentType) : ofByte value.toByte = value := by
  cases value with
  | noOddLotPriceLevelsAttached => decide
  | shortFormOddLotPriceLevelsAttached => decide
  | longFormOddLotPriceLevelsAttached => decide
  | mpidFormOddLotPriceLevelsAttached => decide
  | unlisted byte => simp [ofByte, toByte, byte.property]

def encode (value : OddLotAttachmentType) : List UInt8 :=
  [value.toByte]

def decode : List UInt8 → Option (OddLotAttachmentType × List UInt8)
  | byte :: rest => some (ofByte byte, rest)
  | [] => none

@[simp] theorem encode_length (value : OddLotAttachmentType) : (encode value).length = 1 :=
  rfl

@[simp] theorem decode_encode (value : OddLotAttachmentType) (rest : List UInt8) :
    decode (encode value ++ rest) = some (value, rest) := by
  simp [decode, encode, ofByte_toByte]

end OddLotAttachmentType

/-- Issue Symbol Directory Message: 87 bytes -/
structure IssueSymbolDirectoryMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  symbol : Alpha 11
  oldSymbol : Alpha 11
  issueName : Alpha 30
  issueType : IssueType
  issueSubtype : Alpha 2
  marketTier : MarketTier
  authenticity : Authenticity
  shortSaleThresholdIndicator : ShortSaleThresholdIndicator
  roundLotSize : BitVec 16
  financialStatusIndicator : FinancialStatusIndicator
  deriving DecidableEq, Repr

namespace IssueSymbolDirectoryMessage

def encode (message : IssueSymbolDirectoryMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (Alpha.encode message.symbol
    ++ (Alpha.encode message.oldSymbol
    ++ (Alpha.encode message.issueName
    ++ (IssueType.encode message.issueType
    ++ (Alpha.encode message.issueSubtype
    ++ (MarketTier.encode message.marketTier
    ++ (Authenticity.encode message.authenticity
    ++ (ShortSaleThresholdIndicator.encode message.shortSaleThresholdIndicator
    ++ (encodeUInt 2 message.roundLotSize
    ++ (FinancialStatusIndicator.encode message.financialStatusIndicator))))))))))))))

def decode (bytes : List UInt8) : Option (IssueSymbolDirectoryMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (oldSymbol, bytes) ← Alpha.decode 11 bytes
  let (issueName, bytes) ← Alpha.decode 30 bytes
  let (issueType, bytes) ← IssueType.decode bytes
  let (issueSubtype, bytes) ← Alpha.decode 2 bytes
  let (marketTier, bytes) ← MarketTier.decode bytes
  let (authenticity, bytes) ← Authenticity.decode bytes
  let (shortSaleThresholdIndicator, bytes) ← ShortSaleThresholdIndicator.decode bytes
  let (roundLotSize, bytes) ← decodeUInt 2 bytes
  let (financialStatusIndicator, bytes) ← FinancialStatusIndicator.decode bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, symbol, oldSymbol, issueName, issueType, issueSubtype, marketTier, authenticity, shortSaleThresholdIndicator, roundLotSize, financialStatusIndicator }, bytes)

@[simp] theorem encode_length (message : IssueSymbolDirectoryMessage) : (encode message).length = 87 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length, IssueType.encode_length, MarketTier.encode_length, Authenticity.encode_length, ShortSaleThresholdIndicator.encode_length, FinancialStatusIndicator.encode_length]

theorem encode_length_pos (message : IssueSymbolDirectoryMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : IssueSymbolDirectoryMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, IssueType.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, MarketTier.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Authenticity.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, ShortSaleThresholdIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [FinancialStatusIndicator.decode_encode, some_bind]
  rfl

end IssueSymbolDirectoryMessage

/-- Enhanced Issue Symbol Directory Message: 114 bytes -/
structure EnhancedIssueSymbolDirectoryMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  symbol : Alpha 11
  oldSymbol : Alpha 11
  issueName : Alpha 30
  issueType : IssueType
  issueSubtype : Alpha 2
  marketTier : MarketTier
  authenticity : Authenticity
  shortSaleThresholdIndicator : ShortSaleThresholdIndicator
  roundLotSize : BitVec 16
  financialStatusIndicator : FinancialStatusIndicator
  etpIndicator : Alpha 1
  newIssueIndicator : Alpha 1
  mpiTick : MpiTick
  tradingState : TradingState
  haltReason : Alpha 6
  regShoAction : RegShoAction
  consolidatedPriorDayClosePrice : BitVec 64
  listingMarketOfficialPriorDayClosePrice : BitVec 64
  deriving DecidableEq, Repr

namespace EnhancedIssueSymbolDirectoryMessage

def encode (message : EnhancedIssueSymbolDirectoryMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (Alpha.encode message.symbol
    ++ (Alpha.encode message.oldSymbol
    ++ (Alpha.encode message.issueName
    ++ (IssueType.encode message.issueType
    ++ (Alpha.encode message.issueSubtype
    ++ (MarketTier.encode message.marketTier
    ++ (Authenticity.encode message.authenticity
    ++ (ShortSaleThresholdIndicator.encode message.shortSaleThresholdIndicator
    ++ (encodeUInt 2 message.roundLotSize
    ++ (FinancialStatusIndicator.encode message.financialStatusIndicator
    ++ (Alpha.encode message.etpIndicator
    ++ (Alpha.encode message.newIssueIndicator
    ++ (MpiTick.encode message.mpiTick
    ++ (TradingState.encode message.tradingState
    ++ (Alpha.encode message.haltReason
    ++ (RegShoAction.encode message.regShoAction
    ++ (encodeUInt 8 message.consolidatedPriorDayClosePrice
    ++ (encodeUInt 8 message.listingMarketOfficialPriorDayClosePrice))))))))))))))))))))))

def decode (bytes : List UInt8) : Option (EnhancedIssueSymbolDirectoryMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (oldSymbol, bytes) ← Alpha.decode 11 bytes
  let (issueName, bytes) ← Alpha.decode 30 bytes
  let (issueType, bytes) ← IssueType.decode bytes
  let (issueSubtype, bytes) ← Alpha.decode 2 bytes
  let (marketTier, bytes) ← MarketTier.decode bytes
  let (authenticity, bytes) ← Authenticity.decode bytes
  let (shortSaleThresholdIndicator, bytes) ← ShortSaleThresholdIndicator.decode bytes
  let (roundLotSize, bytes) ← decodeUInt 2 bytes
  let (financialStatusIndicator, bytes) ← FinancialStatusIndicator.decode bytes
  let (etpIndicator, bytes) ← Alpha.decode 1 bytes
  let (newIssueIndicator, bytes) ← Alpha.decode 1 bytes
  let (mpiTick, bytes) ← MpiTick.decode bytes
  let (tradingState, bytes) ← TradingState.decode bytes
  let (haltReason, bytes) ← Alpha.decode 6 bytes
  let (regShoAction, bytes) ← RegShoAction.decode bytes
  let (consolidatedPriorDayClosePrice, bytes) ← decodeUInt 8 bytes
  let (listingMarketOfficialPriorDayClosePrice, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, symbol, oldSymbol, issueName, issueType, issueSubtype, marketTier, authenticity, shortSaleThresholdIndicator, roundLotSize, financialStatusIndicator, etpIndicator, newIssueIndicator, mpiTick, tradingState, haltReason, regShoAction, consolidatedPriorDayClosePrice, listingMarketOfficialPriorDayClosePrice }, bytes)

@[simp] theorem encode_length (message : EnhancedIssueSymbolDirectoryMessage) : (encode message).length = 114 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length, IssueType.encode_length, MarketTier.encode_length, Authenticity.encode_length, ShortSaleThresholdIndicator.encode_length, FinancialStatusIndicator.encode_length, MpiTick.encode_length, TradingState.encode_length, RegShoAction.encode_length]

theorem encode_length_pos (message : EnhancedIssueSymbolDirectoryMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : EnhancedIssueSymbolDirectoryMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, IssueType.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, MarketTier.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Authenticity.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, ShortSaleThresholdIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, FinancialStatusIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, MpiTick.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, TradingState.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, RegShoAction.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end EnhancedIssueSymbolDirectoryMessage

/-- Reg Sho Short Sale Price Test Restricted Indicator Message: 38 bytes -/
structure RegShoShortSalePriceTestRestrictedIndicatorMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  symbol : Alpha 11
  regShoAction : RegShoAction
  deriving DecidableEq, Repr

namespace RegShoShortSalePriceTestRestrictedIndicatorMessage

def encode (message : RegShoShortSalePriceTestRestrictedIndicatorMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (Alpha.encode message.symbol
    ++ (RegShoAction.encode message.regShoAction))))))

def decode (bytes : List UInt8) : Option (RegShoShortSalePriceTestRestrictedIndicatorMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (regShoAction, bytes) ← RegShoAction.decode bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, symbol, regShoAction }, bytes)

@[simp] theorem encode_length (message : RegShoShortSalePriceTestRestrictedIndicatorMessage) : (encode message).length = 38 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length, RegShoAction.encode_length]

theorem encode_length_pos (message : RegShoShortSalePriceTestRestrictedIndicatorMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : RegShoShortSalePriceTestRestrictedIndicatorMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [RegShoAction.decode_encode, some_bind]
  rfl

end RegShoShortSalePriceTestRestrictedIndicatorMessage

/-- Cross Sro Trading Action Message: 56 bytes -/
structure CrossSroTradingActionMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  symbol : Alpha 11
  tradingActionCode : TradingActionCode
  tradingActionSequenceNumber : BitVec 32
  actionTime : BitVec 64
  reasonForTheTradingAction : Alpha 6
  deriving DecidableEq, Repr

namespace CrossSroTradingActionMessage

def encode (message : CrossSroTradingActionMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (Alpha.encode message.symbol
    ++ (TradingActionCode.encode message.tradingActionCode
    ++ (encodeUInt 4 message.tradingActionSequenceNumber
    ++ (encodeUInt 8 message.actionTime
    ++ (Alpha.encode message.reasonForTheTradingAction)))))))))

def decode (bytes : List UInt8) : Option (CrossSroTradingActionMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (tradingActionCode, bytes) ← TradingActionCode.decode bytes
  let (tradingActionSequenceNumber, bytes) ← decodeUInt 4 bytes
  let (actionTime, bytes) ← decodeUInt 8 bytes
  let (reasonForTheTradingAction, bytes) ← Alpha.decode 6 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, symbol, tradingActionCode, tradingActionSequenceNumber, actionTime, reasonForTheTradingAction }, bytes)

@[simp] theorem encode_length (message : CrossSroTradingActionMessage) : (encode message).length = 56 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length, TradingActionCode.encode_length]

theorem encode_length_pos (message : CrossSroTradingActionMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : CrossSroTradingActionMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, TradingActionCode.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [Alpha.decode_encode, some_bind]
  rfl

end CrossSroTradingActionMessage

/-- Market Center Trading Action Message: 47 bytes -/
structure MarketCenterTradingActionMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  symbol : Alpha 11
  tradingActionCode : TradingActionCode
  actionTime : BitVec 64
  marketCenterIdentifier : Alpha 1
  deriving DecidableEq, Repr

namespace MarketCenterTradingActionMessage

def encode (message : MarketCenterTradingActionMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (Alpha.encode message.symbol
    ++ (TradingActionCode.encode message.tradingActionCode
    ++ (encodeUInt 8 message.actionTime
    ++ (Alpha.encode message.marketCenterIdentifier))))))))

def decode (bytes : List UInt8) : Option (MarketCenterTradingActionMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (tradingActionCode, bytes) ← TradingActionCode.decode bytes
  let (actionTime, bytes) ← decodeUInt 8 bytes
  let (marketCenterIdentifier, bytes) ← Alpha.decode 1 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, symbol, tradingActionCode, actionTime, marketCenterIdentifier }, bytes)

@[simp] theorem encode_length (message : MarketCenterTradingActionMessage) : (encode message).length = 47 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length, TradingActionCode.encode_length]

theorem encode_length_pos (message : MarketCenterTradingActionMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : MarketCenterTradingActionMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, TradingActionCode.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [Alpha.decode_encode, some_bind]
  rfl

end MarketCenterTradingActionMessage

/-- Market Wide Circuit Breaker Decline Level Message: 50 bytes -/
structure MarketWideCircuitBreakerDeclineLevelMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  mwcbLevel1 : BitVec 64
  mwcbLevel2 : BitVec 64
  mwcbLevel3 : BitVec 64
  deriving DecidableEq, Repr

namespace MarketWideCircuitBreakerDeclineLevelMessage

def encode (message : MarketWideCircuitBreakerDeclineLevelMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (encodeUInt 8 message.mwcbLevel1
    ++ (encodeUInt 8 message.mwcbLevel2
    ++ (encodeUInt 8 message.mwcbLevel3)))))))

def decode (bytes : List UInt8) : Option (MarketWideCircuitBreakerDeclineLevelMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (mwcbLevel1, bytes) ← decodeUInt 8 bytes
  let (mwcbLevel2, bytes) ← decodeUInt 8 bytes
  let (mwcbLevel3, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, mwcbLevel1, mwcbLevel2, mwcbLevel3 }, bytes)

@[simp] theorem encode_length (message : MarketWideCircuitBreakerDeclineLevelMessage) : (encode message).length = 50 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length]

theorem encode_length_pos (message : MarketWideCircuitBreakerDeclineLevelMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : MarketWideCircuitBreakerDeclineLevelMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end MarketWideCircuitBreakerDeclineLevelMessage

/-- Limit Up Limit Down Price Band Message: 62 bytes -/
structure LimitUpLimitDownPriceBandMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  symbol : Alpha 11
  luldPriceBandIndicator : LuldPriceBandIndicator
  luldPriceBandEffectiveTime : BitVec 64
  limitDownPrice : BitVec 64
  limitUpPrice : BitVec 64
  deriving DecidableEq, Repr

namespace LimitUpLimitDownPriceBandMessage

def encode (message : LimitUpLimitDownPriceBandMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (Alpha.encode message.symbol
    ++ (LuldPriceBandIndicator.encode message.luldPriceBandIndicator
    ++ (encodeUInt 8 message.luldPriceBandEffectiveTime
    ++ (encodeUInt 8 message.limitDownPrice
    ++ (encodeUInt 8 message.limitUpPrice)))))))))

def decode (bytes : List UInt8) : Option (LimitUpLimitDownPriceBandMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (luldPriceBandIndicator, bytes) ← LuldPriceBandIndicator.decode bytes
  let (luldPriceBandEffectiveTime, bytes) ← decodeUInt 8 bytes
  let (limitDownPrice, bytes) ← decodeUInt 8 bytes
  let (limitUpPrice, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, symbol, luldPriceBandIndicator, luldPriceBandEffectiveTime, limitDownPrice, limitUpPrice }, bytes)

@[simp] theorem encode_length (message : LimitUpLimitDownPriceBandMessage) : (encode message).length = 62 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length, LuldPriceBandIndicator.encode_length]

theorem encode_length_pos (message : LimitUpLimitDownPriceBandMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : LimitUpLimitDownPriceBandMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, LuldPriceBandIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end LimitUpLimitDownPriceBandMessage

/-- Auction Collar Message: 66 bytes -/
structure AuctionCollarMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  symbol : Alpha 11
  tradingActionSequenceNumber : BitVec 32
  collarReferencePrice : BitVec 64
  collarUpPrice : BitVec 64
  collarDownPrice : BitVec 64
  collarExtensionIndicator : Alpha 1
  deriving DecidableEq, Repr

namespace AuctionCollarMessage

def encode (message : AuctionCollarMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (Alpha.encode message.symbol
    ++ (encodeUInt 4 message.tradingActionSequenceNumber
    ++ (encodeUInt 8 message.collarReferencePrice
    ++ (encodeUInt 8 message.collarUpPrice
    ++ (encodeUInt 8 message.collarDownPrice
    ++ (Alpha.encode message.collarExtensionIndicator))))))))))

def decode (bytes : List UInt8) : Option (AuctionCollarMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (tradingActionSequenceNumber, bytes) ← decodeUInt 4 bytes
  let (collarReferencePrice, bytes) ← decodeUInt 8 bytes
  let (collarUpPrice, bytes) ← decodeUInt 8 bytes
  let (collarDownPrice, bytes) ← decodeUInt 8 bytes
  let (collarExtensionIndicator, bytes) ← Alpha.decode 1 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, symbol, tradingActionSequenceNumber, collarReferencePrice, collarUpPrice, collarDownPrice, collarExtensionIndicator }, bytes)

@[simp] theorem encode_length (message : AuctionCollarMessage) : (encode message).length = 66 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length]

theorem encode_length_pos (message : AuctionCollarMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : AuctionCollarMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [Alpha.decode_encode, some_bind]
  rfl

end AuctionCollarMessage

/-- Snapshot Sequence Message: 34 bytes -/
structure SnapshotSequenceMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  sequenceNumber : BitVec 64
  deriving DecidableEq, Repr

namespace SnapshotSequenceMessage

def encode (message : SnapshotSequenceMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (encodeUInt 8 message.sequenceNumber)))))

def decode (bytes : List UInt8) : Option (SnapshotSequenceMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (sequenceNumber, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, sequenceNumber }, bytes)

@[simp] theorem encode_length (message : SnapshotSequenceMessage) : (encode message).length = 34 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length]

theorem encode_length_pos (message : SnapshotSequenceMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : SnapshotSequenceMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end SnapshotSequenceMessage

/-- Any Administrative Message Payload, selected by Administrative Message Type -/
inductive AdministrativeMessagePayload where
  | issueSymbolDirectoryMessage (message : IssueSymbolDirectoryMessage) -- "B" 0x42
  | enhancedIssueSymbolDirectoryMessage (message : EnhancedIssueSymbolDirectoryMessage) -- "F" 0x46
  | regShoShortSalePriceTestRestrictedIndicatorMessage (message : RegShoShortSalePriceTestRestrictedIndicatorMessage) -- "V" 0x56
  | crossSroTradingActionMessage (message : CrossSroTradingActionMessage) -- "H" 0x48
  | marketCenterTradingActionMessage (message : MarketCenterTradingActionMessage) -- "K" 0x4B
  | marketWideCircuitBreakerDeclineLevelMessage (message : MarketWideCircuitBreakerDeclineLevelMessage) -- "C" 0x43
  | limitUpLimitDownPriceBandMessage (message : LimitUpLimitDownPriceBandMessage) -- "P" 0x50
  | auctionCollarMessage (message : AuctionCollarMessage) -- "E" 0x45
  | snapshotSequenceMessage (message : SnapshotSequenceMessage) -- "S" 0x53
  deriving DecidableEq, Repr

namespace AdministrativeMessagePayload

/-- The Administrative Message Type each message is sent under -/
def tag : AdministrativeMessagePayload → BitVec 8
  | .issueSymbolDirectoryMessage _ => 66
  | .enhancedIssueSymbolDirectoryMessage _ => 70
  | .regShoShortSalePriceTestRestrictedIndicatorMessage _ => 86
  | .crossSroTradingActionMessage _ => 72
  | .marketCenterTradingActionMessage _ => 75
  | .marketWideCircuitBreakerDeclineLevelMessage _ => 67
  | .limitUpLimitDownPriceBandMessage _ => 80
  | .auctionCollarMessage _ => 69
  | .snapshotSequenceMessage _ => 83

def encode : AdministrativeMessagePayload → List UInt8
  | .issueSymbolDirectoryMessage message => IssueSymbolDirectoryMessage.encode message
  | .enhancedIssueSymbolDirectoryMessage message => EnhancedIssueSymbolDirectoryMessage.encode message
  | .regShoShortSalePriceTestRestrictedIndicatorMessage message => RegShoShortSalePriceTestRestrictedIndicatorMessage.encode message
  | .crossSroTradingActionMessage message => CrossSroTradingActionMessage.encode message
  | .marketCenterTradingActionMessage message => MarketCenterTradingActionMessage.encode message
  | .marketWideCircuitBreakerDeclineLevelMessage message => MarketWideCircuitBreakerDeclineLevelMessage.encode message
  | .limitUpLimitDownPriceBandMessage message => LimitUpLimitDownPriceBandMessage.encode message
  | .auctionCollarMessage message => AuctionCollarMessage.encode message
  | .snapshotSequenceMessage message => SnapshotSequenceMessage.encode message

/-- The most bytes any message's encoding can take -/
theorem encode_length_le (message : AdministrativeMessagePayload) : (encode message).length ≤ 114 := by
  cases message with
  | issueSymbolDirectoryMessage inner =>
    simp only [encode, IssueSymbolDirectoryMessage.encode_length]
    omega
  | enhancedIssueSymbolDirectoryMessage inner =>
    simp only [encode, EnhancedIssueSymbolDirectoryMessage.encode_length]
    omega
  | regShoShortSalePriceTestRestrictedIndicatorMessage inner =>
    simp only [encode, RegShoShortSalePriceTestRestrictedIndicatorMessage.encode_length]
    omega
  | crossSroTradingActionMessage inner =>
    simp only [encode, CrossSroTradingActionMessage.encode_length]
    omega
  | marketCenterTradingActionMessage inner =>
    simp only [encode, MarketCenterTradingActionMessage.encode_length]
    omega
  | marketWideCircuitBreakerDeclineLevelMessage inner =>
    simp only [encode, MarketWideCircuitBreakerDeclineLevelMessage.encode_length]
    omega
  | limitUpLimitDownPriceBandMessage inner =>
    simp only [encode, LimitUpLimitDownPriceBandMessage.encode_length]
    omega
  | auctionCollarMessage inner =>
    simp only [encode, AuctionCollarMessage.encode_length]
    omega
  | snapshotSequenceMessage inner =>
    simp only [encode, SnapshotSequenceMessage.encode_length]
    omega

def decode (tag : BitVec 8) (bytes : List UInt8) : Option (AdministrativeMessagePayload × List UInt8) :=
  if tag = 66 then (IssueSymbolDirectoryMessage.decode bytes).map fun (message, rest) => (.issueSymbolDirectoryMessage message, rest)
  else if tag = 70 then (EnhancedIssueSymbolDirectoryMessage.decode bytes).map fun (message, rest) => (.enhancedIssueSymbolDirectoryMessage message, rest)
  else if tag = 86 then (RegShoShortSalePriceTestRestrictedIndicatorMessage.decode bytes).map fun (message, rest) => (.regShoShortSalePriceTestRestrictedIndicatorMessage message, rest)
  else if tag = 72 then (CrossSroTradingActionMessage.decode bytes).map fun (message, rest) => (.crossSroTradingActionMessage message, rest)
  else if tag = 75 then (MarketCenterTradingActionMessage.decode bytes).map fun (message, rest) => (.marketCenterTradingActionMessage message, rest)
  else if tag = 67 then (MarketWideCircuitBreakerDeclineLevelMessage.decode bytes).map fun (message, rest) => (.marketWideCircuitBreakerDeclineLevelMessage message, rest)
  else if tag = 80 then (LimitUpLimitDownPriceBandMessage.decode bytes).map fun (message, rest) => (.limitUpLimitDownPriceBandMessage message, rest)
  else if tag = 69 then (AuctionCollarMessage.decode bytes).map fun (message, rest) => (.auctionCollarMessage message, rest)
  else if tag = 83 then (SnapshotSequenceMessage.decode bytes).map fun (message, rest) => (.snapshotSequenceMessage message, rest)
  else none

@[simp] theorem decode_encode (message : AdministrativeMessagePayload) (rest : List UInt8) :
    decode (tag message) (encode message ++ rest) = some (message, rest) := by
  cases message <;> simp [decode, encode, tag]

end AdministrativeMessagePayload

/-- Administrative Message -/
structure AdministrativeMessage where
  administrativeMessagePayload : AdministrativeMessagePayload
  deriving DecidableEq, Repr

namespace AdministrativeMessage

def encode (message : AdministrativeMessage) : List UInt8 :=
  encodeUInt 1 (AdministrativeMessagePayload.tag message.administrativeMessagePayload)
    ++ (AdministrativeMessagePayload.encode message.administrativeMessagePayload)

def decode (bytes : List UInt8) : Option (AdministrativeMessage × List UInt8) := do
  let (administrativeMessageType, bytes) ← decodeUInt 1 bytes
  let (administrativeMessagePayload, bytes) ← AdministrativeMessagePayload.decode administrativeMessageType bytes
  pure ({ administrativeMessagePayload }, bytes)

theorem encode_length_pos (message : AdministrativeMessage) : (encode message).length > 0 := by
  unfold encode
  simp only [encodeUInt_length, List.length_append]
  omega

/-- The most bytes an encoding can take -/
theorem encode_length_le (message : AdministrativeMessage) : (encode message).length ≤ 115 := by
  unfold encode
  cases message.administrativeMessagePayload with
  | issueSymbolDirectoryMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, IssueSymbolDirectoryMessage.encode_length]
    omega
  | enhancedIssueSymbolDirectoryMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, EnhancedIssueSymbolDirectoryMessage.encode_length]
    omega
  | regShoShortSalePriceTestRestrictedIndicatorMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, RegShoShortSalePriceTestRestrictedIndicatorMessage.encode_length]
    omega
  | crossSroTradingActionMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, CrossSroTradingActionMessage.encode_length]
    omega
  | marketCenterTradingActionMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, MarketCenterTradingActionMessage.encode_length]
    omega
  | marketWideCircuitBreakerDeclineLevelMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, MarketWideCircuitBreakerDeclineLevelMessage.encode_length]
    omega
  | limitUpLimitDownPriceBandMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, LimitUpLimitDownPriceBandMessage.encode_length]
    omega
  | auctionCollarMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, AuctionCollarMessage.encode_length]
    omega
  | snapshotSequenceMessage inner =>
    simp only [AdministrativeMessagePayload.encode, List.length_append, encodeUInt_length, SnapshotSequenceMessage.encode_length]
    omega

@[simp] theorem decode_encode (message : AdministrativeMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [AdministrativeMessagePayload.decode_encode, some_bind]
  rfl

end AdministrativeMessage

/-- Start Of Day Message: 26 bytes -/
structure StartOfDayMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  deriving DecidableEq, Repr

namespace StartOfDayMessage

def encode (message : StartOfDayMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken))))

def decode (bytes : List UInt8) : Option (StartOfDayMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken }, bytes)

@[simp] theorem encode_length (message : StartOfDayMessage) : (encode message).length = 26 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length]

theorem encode_length_pos (message : StartOfDayMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : StartOfDayMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end StartOfDayMessage

/-- Market Session Open Message: 26 bytes -/
structure MarketSessionOpenMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  deriving DecidableEq, Repr

namespace MarketSessionOpenMessage

def encode (message : MarketSessionOpenMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken))))

def decode (bytes : List UInt8) : Option (MarketSessionOpenMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken }, bytes)

@[simp] theorem encode_length (message : MarketSessionOpenMessage) : (encode message).length = 26 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length]

theorem encode_length_pos (message : MarketSessionOpenMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : MarketSessionOpenMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end MarketSessionOpenMessage

/-- Market Session Close Message: 26 bytes -/
structure MarketSessionCloseMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  deriving DecidableEq, Repr

namespace MarketSessionCloseMessage

def encode (message : MarketSessionCloseMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken))))

def decode (bytes : List UInt8) : Option (MarketSessionCloseMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken }, bytes)

@[simp] theorem encode_length (message : MarketSessionCloseMessage) : (encode message).length = 26 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length]

theorem encode_length_pos (message : MarketSessionCloseMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : MarketSessionCloseMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end MarketSessionCloseMessage

/-- End Of Day Message: 26 bytes -/
structure EndOfDayMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  deriving DecidableEq, Repr

namespace EndOfDayMessage

def encode (message : EndOfDayMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken))))

def decode (bytes : List UInt8) : Option (EndOfDayMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken }, bytes)

@[simp] theorem encode_length (message : EndOfDayMessage) : (encode message).length = 26 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length]

theorem encode_length_pos (message : EndOfDayMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : EndOfDayMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end EndOfDayMessage

/-- End Of Transmissions Message: 26 bytes -/
structure EndOfTransmissionsMessage where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  deriving DecidableEq, Repr

namespace EndOfTransmissionsMessage

def encode (message : EndOfTransmissionsMessage) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken))))

def decode (bytes : List UInt8) : Option (EndOfTransmissionsMessage × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken }, bytes)

@[simp] theorem encode_length (message : EndOfTransmissionsMessage) : (encode message).length = 26 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length]

theorem encode_length_pos (message : EndOfTransmissionsMessage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : EndOfTransmissionsMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end EndOfTransmissionsMessage

/-- Any Control Message Payload, selected by Control Message Type -/
inductive ControlMessagePayload where
  | startOfDayMessage (message : StartOfDayMessage) -- "I" 0x49
  | marketSessionOpenMessage (message : MarketSessionOpenMessage) -- "O" 0x4F
  | marketSessionCloseMessage (message : MarketSessionCloseMessage) -- "C" 0x43
  | endOfDayMessage (message : EndOfDayMessage) -- "J" 0x4A
  | endOfTransmissionsMessage (message : EndOfTransmissionsMessage) -- "Z" 0x5A
  deriving DecidableEq, Repr

namespace ControlMessagePayload

/-- The Control Message Type each message is sent under -/
def tag : ControlMessagePayload → BitVec 8
  | .startOfDayMessage _ => 73
  | .marketSessionOpenMessage _ => 79
  | .marketSessionCloseMessage _ => 67
  | .endOfDayMessage _ => 74
  | .endOfTransmissionsMessage _ => 90

def encode : ControlMessagePayload → List UInt8
  | .startOfDayMessage message => StartOfDayMessage.encode message
  | .marketSessionOpenMessage message => MarketSessionOpenMessage.encode message
  | .marketSessionCloseMessage message => MarketSessionCloseMessage.encode message
  | .endOfDayMessage message => EndOfDayMessage.encode message
  | .endOfTransmissionsMessage message => EndOfTransmissionsMessage.encode message

/-- The most bytes any message's encoding can take -/
theorem encode_length_le (message : ControlMessagePayload) : (encode message).length ≤ 26 := by
  cases message with
  | startOfDayMessage inner =>
    simp only [encode, StartOfDayMessage.encode_length]
    omega
  | marketSessionOpenMessage inner =>
    simp only [encode, MarketSessionOpenMessage.encode_length]
    omega
  | marketSessionCloseMessage inner =>
    simp only [encode, MarketSessionCloseMessage.encode_length]
    omega
  | endOfDayMessage inner =>
    simp only [encode, EndOfDayMessage.encode_length]
    omega
  | endOfTransmissionsMessage inner =>
    simp only [encode, EndOfTransmissionsMessage.encode_length]
    omega

def decode (tag : BitVec 8) (bytes : List UInt8) : Option (ControlMessagePayload × List UInt8) :=
  if tag = 73 then (StartOfDayMessage.decode bytes).map fun (message, rest) => (.startOfDayMessage message, rest)
  else if tag = 79 then (MarketSessionOpenMessage.decode bytes).map fun (message, rest) => (.marketSessionOpenMessage message, rest)
  else if tag = 67 then (MarketSessionCloseMessage.decode bytes).map fun (message, rest) => (.marketSessionCloseMessage message, rest)
  else if tag = 74 then (EndOfDayMessage.decode bytes).map fun (message, rest) => (.endOfDayMessage message, rest)
  else if tag = 90 then (EndOfTransmissionsMessage.decode bytes).map fun (message, rest) => (.endOfTransmissionsMessage message, rest)
  else none

@[simp] theorem decode_encode (message : ControlMessagePayload) (rest : List UInt8) :
    decode (tag message) (encode message ++ rest) = some (message, rest) := by
  cases message <;> simp [decode, encode, tag]

end ControlMessagePayload

/-- Control Message -/
structure ControlMessage where
  controlMessagePayload : ControlMessagePayload
  deriving DecidableEq, Repr

namespace ControlMessage

def encode (message : ControlMessage) : List UInt8 :=
  encodeUInt 1 (ControlMessagePayload.tag message.controlMessagePayload)
    ++ (ControlMessagePayload.encode message.controlMessagePayload)

def decode (bytes : List UInt8) : Option (ControlMessage × List UInt8) := do
  let (controlMessageType, bytes) ← decodeUInt 1 bytes
  let (controlMessagePayload, bytes) ← ControlMessagePayload.decode controlMessageType bytes
  pure ({ controlMessagePayload }, bytes)

theorem encode_length_pos (message : ControlMessage) : (encode message).length > 0 := by
  unfold encode
  simp only [encodeUInt_length, List.length_append]
  omega

/-- The most bytes an encoding can take -/
theorem encode_length_le (message : ControlMessage) : (encode message).length ≤ 27 := by
  unfold encode
  cases message.controlMessagePayload with
  | startOfDayMessage inner =>
    simp only [ControlMessagePayload.encode, List.length_append, encodeUInt_length, StartOfDayMessage.encode_length]
    omega
  | marketSessionOpenMessage inner =>
    simp only [ControlMessagePayload.encode, List.length_append, encodeUInt_length, MarketSessionOpenMessage.encode_length]
    omega
  | marketSessionCloseMessage inner =>
    simp only [ControlMessagePayload.encode, List.length_append, encodeUInt_length, MarketSessionCloseMessage.encode_length]
    omega
  | endOfDayMessage inner =>
    simp only [ControlMessagePayload.encode, List.length_append, encodeUInt_length, EndOfDayMessage.encode_length]
    omega
  | endOfTransmissionsMessage inner =>
    simp only [ControlMessagePayload.encode, List.length_append, encodeUInt_length, EndOfTransmissionsMessage.encode_length]
    omega

@[simp] theorem decode_encode (message : ControlMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [ControlMessagePayload.decode_encode, some_bind]
  rfl

end ControlMessage

/-- National Bbo Appendage Longform: 27 bytes -/
structure NationalBboAppendageLongform where
  nbboQuoteCondition : Alpha 1
  nationalBestBidMarketCenter : Alpha 1
  nationalBestBidPrice : BitVec 64
  nationalBestBidSize : BitVec 32
  nationalBestAskMarketCenter : Alpha 1
  nationalBestAskPrice : BitVec 64
  nationalBestAskSize : BitVec 32
  deriving DecidableEq, Repr

namespace NationalBboAppendageLongform

def encode (message : NationalBboAppendageLongform) : List UInt8 :=
  Alpha.encode message.nbboQuoteCondition
    ++ (Alpha.encode message.nationalBestBidMarketCenter
    ++ (encodeUInt 8 message.nationalBestBidPrice
    ++ (encodeUInt 4 message.nationalBestBidSize
    ++ (Alpha.encode message.nationalBestAskMarketCenter
    ++ (encodeUInt 8 message.nationalBestAskPrice
    ++ (encodeUInt 4 message.nationalBestAskSize))))))

def decode (bytes : List UInt8) : Option (NationalBboAppendageLongform × List UInt8) := do
  let (nbboQuoteCondition, bytes) ← Alpha.decode 1 bytes
  let (nationalBestBidMarketCenter, bytes) ← Alpha.decode 1 bytes
  let (nationalBestBidPrice, bytes) ← decodeUInt 8 bytes
  let (nationalBestBidSize, bytes) ← decodeUInt 4 bytes
  let (nationalBestAskMarketCenter, bytes) ← Alpha.decode 1 bytes
  let (nationalBestAskPrice, bytes) ← decodeUInt 8 bytes
  let (nationalBestAskSize, bytes) ← decodeUInt 4 bytes
  pure ({ nbboQuoteCondition, nationalBestBidMarketCenter, nationalBestBidPrice, nationalBestBidSize, nationalBestAskMarketCenter, nationalBestAskPrice, nationalBestAskSize }, bytes)

@[simp] theorem encode_length (message : NationalBboAppendageLongform) : (encode message).length = 27 := by
  unfold encode
  simp only [List.length_append, Alpha.encode_length, encodeUInt_length]

theorem encode_length_pos (message : NationalBboAppendageLongform) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : NationalBboAppendageLongform) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [decodeUInt_encodeUInt, some_bind]
  rfl

end NationalBboAppendageLongform

/-- Finra Adf Mpid Appendage: 8 bytes -/
structure FinraAdfMpidAppendage where
  bidAdfMpid : Alpha 4
  askAdfMpid : Alpha 4
  deriving DecidableEq, Repr

namespace FinraAdfMpidAppendage

def encode (message : FinraAdfMpidAppendage) : List UInt8 :=
  Alpha.encode message.bidAdfMpid
    ++ (Alpha.encode message.askAdfMpid)

def decode (bytes : List UInt8) : Option (FinraAdfMpidAppendage × List UInt8) := do
  let (bidAdfMpid, bytes) ← Alpha.decode 4 bytes
  let (askAdfMpid, bytes) ← Alpha.decode 4 bytes
  pure ({ bidAdfMpid, askAdfMpid }, bytes)

@[simp] theorem encode_length (message : FinraAdfMpidAppendage) : (encode message).length = 8 := by
  unfold encode
  simp only [List.length_append, Alpha.encode_length]

theorem encode_length_pos (message : FinraAdfMpidAppendage) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : FinraAdfMpidAppendage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [Alpha.decode_encode, some_bind]
  rfl

end FinraAdfMpidAppendage

/-- Bolo Appendage Mpid Form: 30 bytes -/
structure BoloAppendageMpidForm where
  boloBestBidMarketCenter : Alpha 1
  boloBidPrice : BitVec 64
  boloBidSize : BitVec 16
  boloBestAskMarketCenter : Alpha 1
  boloAskPrice : BitVec 64
  boloAskSize : BitVec 16
  boloBestBidMarketParticipantIdentifier : Alpha 4
  boloBestAskMarketParticipantIdentifier : Alpha 4
  deriving DecidableEq, Repr

namespace BoloAppendageMpidForm

def encode (message : BoloAppendageMpidForm) : List UInt8 :=
  Alpha.encode message.boloBestBidMarketCenter
    ++ (encodeUInt 8 message.boloBidPrice
    ++ (encodeUInt 2 message.boloBidSize
    ++ (Alpha.encode message.boloBestAskMarketCenter
    ++ (encodeUInt 8 message.boloAskPrice
    ++ (encodeUInt 2 message.boloAskSize
    ++ (Alpha.encode message.boloBestBidMarketParticipantIdentifier
    ++ (Alpha.encode message.boloBestAskMarketParticipantIdentifier)))))))

def decode (bytes : List UInt8) : Option (BoloAppendageMpidForm × List UInt8) := do
  let (boloBestBidMarketCenter, bytes) ← Alpha.decode 1 bytes
  let (boloBidPrice, bytes) ← decodeUInt 8 bytes
  let (boloBidSize, bytes) ← decodeUInt 2 bytes
  let (boloBestAskMarketCenter, bytes) ← Alpha.decode 1 bytes
  let (boloAskPrice, bytes) ← decodeUInt 8 bytes
  let (boloAskSize, bytes) ← decodeUInt 2 bytes
  let (boloBestBidMarketParticipantIdentifier, bytes) ← Alpha.decode 4 bytes
  let (boloBestAskMarketParticipantIdentifier, bytes) ← Alpha.decode 4 bytes
  pure ({ boloBestBidMarketCenter, boloBidPrice, boloBidSize, boloBestAskMarketCenter, boloAskPrice, boloAskSize, boloBestBidMarketParticipantIdentifier, boloBestAskMarketParticipantIdentifier }, bytes)

@[simp] theorem encode_length (message : BoloAppendageMpidForm) : (encode message).length = 30 := by
  unfold encode
  simp only [List.length_append, Alpha.encode_length, encodeUInt_length]

theorem encode_length_pos (message : BoloAppendageMpidForm) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : BoloAppendageMpidForm) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [Alpha.decode_encode, some_bind]
  rfl

end BoloAppendageMpidForm

/-- Utp Combined Quote Message Long Form: 145 bytes -/
structure UtpCombinedQuoteMessageLongForm where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  finraAdfTimestamp : BitVec 64
  symbol : Alpha 11
  bidPrice : BitVec 64
  bidSize : BitVec 32
  askPrice : BitVec 64
  askSize : BitVec 32
  quoteCondition : QuoteCondition
  sipGeneratedUpdateFlag : SipGeneratedUpdateFlag
  luldBboIndicator : LuldBboIndicator
  retailInterestIndicator : RetailInterestIndicator
  nbboAppendageIndicator : NbboAppendageIndicator
  luldNationalBboIndicator : LuldNationalBboIndicator
  finraAdfMpidAppendageIndicator : FinraAdfMpidAppendageIndicator
  boloAppendageIndicator : BoloAppendageIndicator
  oddLotAttachmentType : OddLotAttachmentType
  oddLotAttachmentCount : BitVec 16
  nationalBboAppendageLongform : NationalBboAppendageLongform
  finraAdfMpidAppendage : FinraAdfMpidAppendage
  boloAppendageMpidForm : BoloAppendageMpidForm
  deriving DecidableEq, Repr

namespace UtpCombinedQuoteMessageLongForm

def encode (message : UtpCombinedQuoteMessageLongForm) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (encodeUInt 8 message.finraAdfTimestamp
    ++ (Alpha.encode message.symbol
    ++ (encodeUInt 8 message.bidPrice
    ++ (encodeUInt 4 message.bidSize
    ++ (encodeUInt 8 message.askPrice
    ++ (encodeUInt 4 message.askSize
    ++ (QuoteCondition.encode message.quoteCondition
    ++ (SipGeneratedUpdateFlag.encode message.sipGeneratedUpdateFlag
    ++ (LuldBboIndicator.encode message.luldBboIndicator
    ++ (RetailInterestIndicator.encode message.retailInterestIndicator
    ++ (NbboAppendageIndicator.encode message.nbboAppendageIndicator
    ++ (LuldNationalBboIndicator.encode message.luldNationalBboIndicator
    ++ (FinraAdfMpidAppendageIndicator.encode message.finraAdfMpidAppendageIndicator
    ++ (BoloAppendageIndicator.encode message.boloAppendageIndicator
    ++ (OddLotAttachmentType.encode message.oddLotAttachmentType
    ++ (encodeUInt 2 message.oddLotAttachmentCount
    ++ (NationalBboAppendageLongform.encode message.nationalBboAppendageLongform
    ++ (FinraAdfMpidAppendage.encode message.finraAdfMpidAppendage
    ++ (BoloAppendageMpidForm.encode message.boloAppendageMpidForm)))))))))))))))))))))))

def decode (bytes : List UInt8) : Option (UtpCombinedQuoteMessageLongForm × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (finraAdfTimestamp, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (bidPrice, bytes) ← decodeUInt 8 bytes
  let (bidSize, bytes) ← decodeUInt 4 bytes
  let (askPrice, bytes) ← decodeUInt 8 bytes
  let (askSize, bytes) ← decodeUInt 4 bytes
  let (quoteCondition, bytes) ← QuoteCondition.decode bytes
  let (sipGeneratedUpdateFlag, bytes) ← SipGeneratedUpdateFlag.decode bytes
  let (luldBboIndicator, bytes) ← LuldBboIndicator.decode bytes
  let (retailInterestIndicator, bytes) ← RetailInterestIndicator.decode bytes
  let (nbboAppendageIndicator, bytes) ← NbboAppendageIndicator.decode bytes
  let (luldNationalBboIndicator, bytes) ← LuldNationalBboIndicator.decode bytes
  let (finraAdfMpidAppendageIndicator, bytes) ← FinraAdfMpidAppendageIndicator.decode bytes
  let (boloAppendageIndicator, bytes) ← BoloAppendageIndicator.decode bytes
  let (oddLotAttachmentType, bytes) ← OddLotAttachmentType.decode bytes
  let (oddLotAttachmentCount, bytes) ← decodeUInt 2 bytes
  let (nationalBboAppendageLongform, bytes) ← NationalBboAppendageLongform.decode bytes
  let (finraAdfMpidAppendage, bytes) ← FinraAdfMpidAppendage.decode bytes
  let (boloAppendageMpidForm, bytes) ← BoloAppendageMpidForm.decode bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, finraAdfTimestamp, symbol, bidPrice, bidSize, askPrice, askSize, quoteCondition, sipGeneratedUpdateFlag, luldBboIndicator, retailInterestIndicator, nbboAppendageIndicator, luldNationalBboIndicator, finraAdfMpidAppendageIndicator, boloAppendageIndicator, oddLotAttachmentType, oddLotAttachmentCount, nationalBboAppendageLongform, finraAdfMpidAppendage, boloAppendageMpidForm }, bytes)

@[simp] theorem encode_length (message : UtpCombinedQuoteMessageLongForm) : (encode message).length = 145 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length, QuoteCondition.encode_length, SipGeneratedUpdateFlag.encode_length, LuldBboIndicator.encode_length, RetailInterestIndicator.encode_length, NbboAppendageIndicator.encode_length, LuldNationalBboIndicator.encode_length, FinraAdfMpidAppendageIndicator.encode_length, BoloAppendageIndicator.encode_length, OddLotAttachmentType.encode_length, NationalBboAppendageLongform.encode_length, FinraAdfMpidAppendage.encode_length, BoloAppendageMpidForm.encode_length]

theorem encode_length_pos (message : UtpCombinedQuoteMessageLongForm) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : UtpCombinedQuoteMessageLongForm) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, QuoteCondition.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SipGeneratedUpdateFlag.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, LuldBboIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, RetailInterestIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, NbboAppendageIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, LuldNationalBboIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, FinraAdfMpidAppendageIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, BoloAppendageIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, OddLotAttachmentType.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, NationalBboAppendageLongform.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, FinraAdfMpidAppendage.decode_encode, some_bind]
  dsimp only
  rw [BoloAppendageMpidForm.decode_encode, some_bind]
  rfl

end UtpCombinedQuoteMessageLongForm

/-- Odd Lot Quote Message Long Form: 80 bytes -/
structure OddLotQuoteMessageLongForm where
  marketCenterOriginator : MarketCenterOriginator
  subMarketCenterId : SubMarketCenterId
  sipTimestamp : BitVec 64
  timestamp1 : BitVec 64
  participantToken : BitVec 64
  adfTimestamp : BitVec 64
  symbol : Alpha 11
  sipGeneratedUpdateFlag : SipGeneratedUpdateFlag
  boloAppendageIndicator : BoloAppendageIndicator
  oddLotAttachmentType : OddLotAttachmentType
  oddLotAttachmentCount : BitVec 16
  boloAppendageMpidForm : BoloAppendageMpidForm
  deriving DecidableEq, Repr

namespace OddLotQuoteMessageLongForm

def encode (message : OddLotQuoteMessageLongForm) : List UInt8 :=
  MarketCenterOriginator.encode message.marketCenterOriginator
    ++ (SubMarketCenterId.encode message.subMarketCenterId
    ++ (encodeUInt 8 message.sipTimestamp
    ++ (encodeUInt 8 message.timestamp1
    ++ (encodeUInt 8 message.participantToken
    ++ (encodeUInt 8 message.adfTimestamp
    ++ (Alpha.encode message.symbol
    ++ (SipGeneratedUpdateFlag.encode message.sipGeneratedUpdateFlag
    ++ (BoloAppendageIndicator.encode message.boloAppendageIndicator
    ++ (OddLotAttachmentType.encode message.oddLotAttachmentType
    ++ (encodeUInt 2 message.oddLotAttachmentCount
    ++ (BoloAppendageMpidForm.encode message.boloAppendageMpidForm)))))))))))

def decode (bytes : List UInt8) : Option (OddLotQuoteMessageLongForm × List UInt8) := do
  let (marketCenterOriginator, bytes) ← MarketCenterOriginator.decode bytes
  let (subMarketCenterId, bytes) ← SubMarketCenterId.decode bytes
  let (sipTimestamp, bytes) ← decodeUInt 8 bytes
  let (timestamp1, bytes) ← decodeUInt 8 bytes
  let (participantToken, bytes) ← decodeUInt 8 bytes
  let (adfTimestamp, bytes) ← decodeUInt 8 bytes
  let (symbol, bytes) ← Alpha.decode 11 bytes
  let (sipGeneratedUpdateFlag, bytes) ← SipGeneratedUpdateFlag.decode bytes
  let (boloAppendageIndicator, bytes) ← BoloAppendageIndicator.decode bytes
  let (oddLotAttachmentType, bytes) ← OddLotAttachmentType.decode bytes
  let (oddLotAttachmentCount, bytes) ← decodeUInt 2 bytes
  let (boloAppendageMpidForm, bytes) ← BoloAppendageMpidForm.decode bytes
  pure ({ marketCenterOriginator, subMarketCenterId, sipTimestamp, timestamp1, participantToken, adfTimestamp, symbol, sipGeneratedUpdateFlag, boloAppendageIndicator, oddLotAttachmentType, oddLotAttachmentCount, boloAppendageMpidForm }, bytes)

@[simp] theorem encode_length (message : OddLotQuoteMessageLongForm) : (encode message).length = 80 := by
  unfold encode
  simp only [List.length_append, MarketCenterOriginator.encode_length, SubMarketCenterId.encode_length, encodeUInt_length, Alpha.encode_length, SipGeneratedUpdateFlag.encode_length, BoloAppendageIndicator.encode_length, OddLotAttachmentType.encode_length, BoloAppendageMpidForm.encode_length]

theorem encode_length_pos (message : OddLotQuoteMessageLongForm) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : OddLotQuoteMessageLongForm) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, MarketCenterOriginator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SubMarketCenterId.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, SipGeneratedUpdateFlag.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, BoloAppendageIndicator.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, OddLotAttachmentType.decode_encode, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [BoloAppendageMpidForm.decode_encode, some_bind]
  rfl

end OddLotQuoteMessageLongForm

/-- Any Quote Message Payload, selected by Quote Message Type -/
inductive QuoteMessagePayload where
  | utpCombinedQuoteMessageLongForm (message : UtpCombinedQuoteMessageLongForm) -- "D" 0x44
  | oddLotQuoteMessageLongForm (message : OddLotQuoteMessageLongForm) -- "B" 0x42
  deriving DecidableEq, Repr

namespace QuoteMessagePayload

/-- The Quote Message Type each message is sent under -/
def tag : QuoteMessagePayload → BitVec 8
  | .utpCombinedQuoteMessageLongForm _ => 68
  | .oddLotQuoteMessageLongForm _ => 66

def encode : QuoteMessagePayload → List UInt8
  | .utpCombinedQuoteMessageLongForm message => UtpCombinedQuoteMessageLongForm.encode message
  | .oddLotQuoteMessageLongForm message => OddLotQuoteMessageLongForm.encode message

/-- The most bytes any message's encoding can take -/
theorem encode_length_le (message : QuoteMessagePayload) : (encode message).length ≤ 145 := by
  cases message with
  | utpCombinedQuoteMessageLongForm inner =>
    simp only [encode, UtpCombinedQuoteMessageLongForm.encode_length]
    omega
  | oddLotQuoteMessageLongForm inner =>
    simp only [encode, OddLotQuoteMessageLongForm.encode_length]
    omega

def decode (tag : BitVec 8) (bytes : List UInt8) : Option (QuoteMessagePayload × List UInt8) :=
  if tag = 68 then (UtpCombinedQuoteMessageLongForm.decode bytes).map fun (message, rest) => (.utpCombinedQuoteMessageLongForm message, rest)
  else if tag = 66 then (OddLotQuoteMessageLongForm.decode bytes).map fun (message, rest) => (.oddLotQuoteMessageLongForm message, rest)
  else none

@[simp] theorem decode_encode (message : QuoteMessagePayload) (rest : List UInt8) :
    decode (tag message) (encode message ++ rest) = some (message, rest) := by
  cases message <;> simp [decode, encode, tag]

end QuoteMessagePayload

/-- Quote Message -/
structure QuoteMessage where
  quoteMessagePayload : QuoteMessagePayload
  deriving DecidableEq, Repr

namespace QuoteMessage

def encode (message : QuoteMessage) : List UInt8 :=
  encodeUInt 1 (QuoteMessagePayload.tag message.quoteMessagePayload)
    ++ (QuoteMessagePayload.encode message.quoteMessagePayload)

def decode (bytes : List UInt8) : Option (QuoteMessage × List UInt8) := do
  let (quoteMessageType, bytes) ← decodeUInt 1 bytes
  let (quoteMessagePayload, bytes) ← QuoteMessagePayload.decode quoteMessageType bytes
  pure ({ quoteMessagePayload }, bytes)

theorem encode_length_pos (message : QuoteMessage) : (encode message).length > 0 := by
  unfold encode
  simp only [encodeUInt_length, List.length_append]
  omega

/-- The most bytes an encoding can take -/
theorem encode_length_le (message : QuoteMessage) : (encode message).length ≤ 146 := by
  unfold encode
  cases message.quoteMessagePayload with
  | utpCombinedQuoteMessageLongForm inner =>
    simp only [QuoteMessagePayload.encode, List.length_append, encodeUInt_length, UtpCombinedQuoteMessageLongForm.encode_length]
    omega
  | oddLotQuoteMessageLongForm inner =>
    simp only [QuoteMessagePayload.encode, List.length_append, encodeUInt_length, OddLotQuoteMessageLongForm.encode_length]
    omega

@[simp] theorem decode_encode (message : QuoteMessage) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [QuoteMessagePayload.decode_encode, some_bind]
  rfl

end QuoteMessage

/-- Any Category Payload, selected by Message Category -/
inductive CategoryPayload where
  | administrativeMessage (message : AdministrativeMessage) -- "A" 0x41
  | controlMessage (message : ControlMessage) -- "C" 0x43
  | quoteMessage (message : QuoteMessage) -- "Q" 0x51
  deriving DecidableEq, Repr

namespace CategoryPayload

/-- The Message Category each message is sent under -/
def tag : CategoryPayload → BitVec 8
  | .administrativeMessage _ => 65
  | .controlMessage _ => 67
  | .quoteMessage _ => 81

def encode : CategoryPayload → List UInt8
  | .administrativeMessage message => AdministrativeMessage.encode message
  | .controlMessage message => ControlMessage.encode message
  | .quoteMessage message => QuoteMessage.encode message

/-- The most bytes any message's encoding can take -/
theorem encode_length_le (message : CategoryPayload) : (encode message).length ≤ 146 := by
  cases message with
  | administrativeMessage inner =>
    have bound_inner := AdministrativeMessage.encode_length_le inner
    simp only [encode]
    omega
  | controlMessage inner =>
    have bound_inner := ControlMessage.encode_length_le inner
    simp only [encode]
    omega
  | quoteMessage inner =>
    have bound_inner := QuoteMessage.encode_length_le inner
    simp only [encode]
    omega

def decode (tag : BitVec 8) (bytes : List UInt8) : Option (CategoryPayload × List UInt8) :=
  if tag = 65 then (AdministrativeMessage.decode bytes).map fun (message, rest) => (.administrativeMessage message, rest)
  else if tag = 67 then (ControlMessage.decode bytes).map fun (message, rest) => (.controlMessage message, rest)
  else if tag = 81 then (QuoteMessage.decode bytes).map fun (message, rest) => (.quoteMessage message, rest)
  else none

@[simp] theorem decode_encode (message : CategoryPayload) (rest : List UInt8) :
    decode (tag message) (encode message ++ rest) = some (message, rest) := by
  cases message <;> simp [decode, encode, tag]

end CategoryPayload

/-- Sequenced Data Packet -/
structure SequencedDataPacket where
  version : BitVec 8
  categoryPayload : CategoryPayload
  deriving DecidableEq, Repr

namespace SequencedDataPacket

def encode (message : SequencedDataPacket) : List UInt8 :=
  encodeUInt 1 message.version
    ++ (encodeUInt 1 (CategoryPayload.tag message.categoryPayload)
    ++ (CategoryPayload.encode message.categoryPayload))

def decode (bytes : List UInt8) : Option (SequencedDataPacket × List UInt8) := do
  let (version, bytes) ← decodeUInt 1 bytes
  let (messageCategory, bytes) ← decodeUInt 1 bytes
  let (categoryPayload, bytes) ← CategoryPayload.decode messageCategory bytes
  pure ({ version, categoryPayload }, bytes)

theorem encode_length_pos (message : SequencedDataPacket) : (encode message).length > 0 := by
  unfold encode
  simp only [encodeUInt_length, List.length_append, ← Nat.add_assoc]
  omega

/-- The most bytes an encoding can take -/
theorem encode_length_le (message : SequencedDataPacket) : (encode message).length ≤ 148 := by
  unfold encode
  cases message.categoryPayload with
  | administrativeMessage inner =>
    have bound_inner := AdministrativeMessage.encode_length_le inner
    simp only [CategoryPayload.encode, List.length_append, ← Nat.add_assoc, encodeUInt_length]
    omega
  | controlMessage inner =>
    have bound_inner := ControlMessage.encode_length_le inner
    simp only [CategoryPayload.encode, List.length_append, ← Nat.add_assoc, encodeUInt_length]
    omega
  | quoteMessage inner =>
    have bound_inner := QuoteMessage.encode_length_le inner
    simp only [CategoryPayload.encode, List.length_append, ← Nat.add_assoc, encodeUInt_length]
    omega

@[simp] theorem decode_encode (message : SequencedDataPacket) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [CategoryPayload.decode_encode, some_bind]
  rfl

end SequencedDataPacket

/-- Debug Packet: 1 bytes -/
structure DebugPacket where
  text : Alpha 1
  deriving DecidableEq, Repr

namespace DebugPacket

def encode (message : DebugPacket) : List UInt8 :=
  Alpha.encode message.text

def decode (bytes : List UInt8) : Option (DebugPacket × List UInt8) := do
  let (text, bytes) ← Alpha.decode 1 bytes
  pure ({ text }, bytes)

@[simp] theorem encode_length (message : DebugPacket) : (encode message).length = 1 := by
  unfold encode
  simp only [Alpha.encode_length]

theorem encode_length_pos (message : DebugPacket) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : DebugPacket) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [Alpha.decode_encode, some_bind]
  rfl

end DebugPacket

/-- Login Accepted Packet: 30 bytes -/
structure LoginAcceptedPacket where
  acceptedSession : Alpha 10
  acceptedSequenceNumber : Alpha 20
  deriving DecidableEq, Repr

namespace LoginAcceptedPacket

def encode (message : LoginAcceptedPacket) : List UInt8 :=
  Alpha.encode message.acceptedSession
    ++ (Alpha.encode message.acceptedSequenceNumber)

def decode (bytes : List UInt8) : Option (LoginAcceptedPacket × List UInt8) := do
  let (acceptedSession, bytes) ← Alpha.decode 10 bytes
  let (acceptedSequenceNumber, bytes) ← Alpha.decode 20 bytes
  pure ({ acceptedSession, acceptedSequenceNumber }, bytes)

@[simp] theorem encode_length (message : LoginAcceptedPacket) : (encode message).length = 30 := by
  unfold encode
  simp only [List.length_append, Alpha.encode_length]

theorem encode_length_pos (message : LoginAcceptedPacket) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : LoginAcceptedPacket) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [List.append_assoc, Alpha.decode_encode, some_bind]
  dsimp only
  rw [Alpha.decode_encode, some_bind]
  rfl

end LoginAcceptedPacket

/-- Login Rejected Packet: 1 bytes -/
structure LoginRejectedPacket where
  rejectReasonCode : Alpha 1
  deriving DecidableEq, Repr

namespace LoginRejectedPacket

def encode (message : LoginRejectedPacket) : List UInt8 :=
  Alpha.encode message.rejectReasonCode

def decode (bytes : List UInt8) : Option (LoginRejectedPacket × List UInt8) := do
  let (rejectReasonCode, bytes) ← Alpha.decode 1 bytes
  pure ({ rejectReasonCode }, bytes)

@[simp] theorem encode_length (message : LoginRejectedPacket) : (encode message).length = 1 := by
  unfold encode
  simp only [Alpha.encode_length]

theorem encode_length_pos (message : LoginRejectedPacket) : (encode message).length > 0 := by
  rw [encode_length]
  decide

@[simp] theorem decode_encode (message : LoginRejectedPacket) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  unfold decode encode
  rw [Alpha.decode_encode, some_bind]
  rfl

end LoginRejectedPacket

/-- Server Heartbeat Packet: 0 bytes -/
structure ServerHeartbeatPacket where
  deriving DecidableEq, Repr

namespace ServerHeartbeatPacket

def encode (_ : ServerHeartbeatPacket) : List UInt8 :=
  []

def decode (bytes : List UInt8) : Option (ServerHeartbeatPacket × List UInt8) :=
  some (⟨⟩, bytes)

@[simp] theorem encode_length (message : ServerHeartbeatPacket) : (encode message).length = 0 := by
  simp [encode]

@[simp] theorem decode_encode (message : ServerHeartbeatPacket) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  simp [decode, encode]

end ServerHeartbeatPacket

/-- End Of Session Packet: 0 bytes -/
structure EndOfSessionPacket where
  deriving DecidableEq, Repr

namespace EndOfSessionPacket

def encode (_ : EndOfSessionPacket) : List UInt8 :=
  []

def decode (bytes : List UInt8) : Option (EndOfSessionPacket × List UInt8) :=
  some (⟨⟩, bytes)

@[simp] theorem encode_length (message : EndOfSessionPacket) : (encode message).length = 0 := by
  simp [encode]

@[simp] theorem decode_encode (message : EndOfSessionPacket) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) := by
  simp [decode, encode]

end EndOfSessionPacket

/-- Any Server Tcp Payload, selected by Server Packet Type -/
inductive ServerTcpPayload where
  | sequencedDataPacket (message : SequencedDataPacket) -- "S" 0x53
  | debugPacket (message : DebugPacket) -- "+" 0x2B
  | loginAcceptedPacket (message : LoginAcceptedPacket) -- "A" 0x41
  | loginRejectedPacket (message : LoginRejectedPacket) -- "J" 0x4A
  | serverHeartbeatPacket (message : ServerHeartbeatPacket) -- "H" 0x48
  | endOfSessionPacket (message : EndOfSessionPacket) -- "Z" 0x5A
  deriving DecidableEq, Repr

namespace ServerTcpPayload

/-- The Server Packet Type each message is sent under -/
def tag : ServerTcpPayload → BitVec 8
  | .sequencedDataPacket _ => 83
  | .debugPacket _ => 43
  | .loginAcceptedPacket _ => 65
  | .loginRejectedPacket _ => 74
  | .serverHeartbeatPacket _ => 72
  | .endOfSessionPacket _ => 90

def encode : ServerTcpPayload → List UInt8
  | .sequencedDataPacket message => SequencedDataPacket.encode message
  | .debugPacket message => DebugPacket.encode message
  | .loginAcceptedPacket message => LoginAcceptedPacket.encode message
  | .loginRejectedPacket message => LoginRejectedPacket.encode message
  | .serverHeartbeatPacket message => ServerHeartbeatPacket.encode message
  | .endOfSessionPacket message => EndOfSessionPacket.encode message

/-- The most bytes any message's encoding can take -/
theorem encode_length_le (message : ServerTcpPayload) : (encode message).length ≤ 148 := by
  cases message with
  | sequencedDataPacket inner =>
    have bound_inner := SequencedDataPacket.encode_length_le inner
    simp only [encode]
    omega
  | debugPacket inner =>
    simp only [encode, DebugPacket.encode_length]
    omega
  | loginAcceptedPacket inner =>
    simp only [encode, LoginAcceptedPacket.encode_length]
    omega
  | loginRejectedPacket inner =>
    simp only [encode, LoginRejectedPacket.encode_length]
    omega
  | serverHeartbeatPacket inner =>
    simp only [encode, ServerHeartbeatPacket.encode_length]
    omega
  | endOfSessionPacket inner =>
    simp only [encode, EndOfSessionPacket.encode_length]
    omega

def decode (tag : BitVec 8) (bytes : List UInt8) : Option (ServerTcpPayload × List UInt8) :=
  if tag = 83 then (SequencedDataPacket.decode bytes).map fun (message, rest) => (.sequencedDataPacket message, rest)
  else if tag = 43 then (DebugPacket.decode bytes).map fun (message, rest) => (.debugPacket message, rest)
  else if tag = 65 then (LoginAcceptedPacket.decode bytes).map fun (message, rest) => (.loginAcceptedPacket message, rest)
  else if tag = 74 then (LoginRejectedPacket.decode bytes).map fun (message, rest) => (.loginRejectedPacket message, rest)
  else if tag = 72 then (ServerHeartbeatPacket.decode bytes).map fun (message, rest) => (.serverHeartbeatPacket message, rest)
  else if tag = 90 then (EndOfSessionPacket.decode bytes).map fun (message, rest) => (.endOfSessionPacket message, rest)
  else none

@[simp] theorem decode_encode (message : ServerTcpPayload) (rest : List UInt8) :
    decode (tag message) (encode message ++ rest) = some (message, rest) := by
  cases message <;> simp [decode, encode, tag]

end ServerTcpPayload

/-- Server Packet -/
structure ServerPacket where
  serverTcpPayload : ServerTcpPayload
  deriving DecidableEq, Repr

namespace ServerPacket

def encodeBody (message : ServerPacket) : List UInt8 :=
  encodeUInt 1 (ServerTcpPayload.tag message.serverTcpPayload)
    ++ (ServerTcpPayload.encode message.serverTcpPayload)

def decodeBody (bytes : List UInt8) : Option (ServerPacket × List UInt8) := do
  let (serverPacketType, bytes) ← decodeUInt 1 bytes
  let (serverTcpPayload, bytes) ← ServerTcpPayload.decode serverPacketType bytes
  pure ({ serverTcpPayload }, bytes)

theorem decodeBody_encodeBody (message : ServerPacket) (rest : List UInt8) :
    decodeBody (encodeBody message ++ rest) = some (message, rest) := by
  unfold decodeBody encodeBody
  rw [List.append_assoc, decodeUInt_encodeUInt, some_bind]
  dsimp only
  rw [ServerTcpPayload.decode_encode, some_bind]
  rfl

/-- Every body fits the length prefix -/
theorem encodeBody_length_lt (message : ServerPacket) : (encodeBody message).length + 0 < 256 ^ 2 := by
  unfold encodeBody
  cases message.serverTcpPayload with
  | sequencedDataPacket inner =>
    have bound_inner := SequencedDataPacket.encode_length_le inner
    simp only [ServerTcpPayload.encode, List.length_append, encodeUInt_length]
    omega
  | debugPacket inner =>
    simp only [ServerTcpPayload.encode, List.length_append, encodeUInt_length, DebugPacket.encode_length]
    omega
  | loginAcceptedPacket inner =>
    simp only [ServerTcpPayload.encode, List.length_append, encodeUInt_length, LoginAcceptedPacket.encode_length]
    omega
  | loginRejectedPacket inner =>
    simp only [ServerTcpPayload.encode, List.length_append, encodeUInt_length, LoginRejectedPacket.encode_length]
    omega
  | serverHeartbeatPacket inner =>
    simp only [ServerTcpPayload.encode, List.length_append, encodeUInt_length, ServerHeartbeatPacket.encode_length]
    omega
  | endOfSessionPacket inner =>
    simp only [ServerTcpPayload.encode, List.length_append, encodeUInt_length, EndOfSessionPacket.encode_length]
    omega

/-- Size rule: Packet Length counts the bytes after it, so it is written from the body and checked on decode -/
def encode : ServerPacket → List UInt8 :=
  encodeFramed 2 0 encodeBody

def decode : List UInt8 → Option (ServerPacket × List UInt8) :=
  decodeFramed 2 0 decodeBody

@[simp] theorem decode_encode (message : ServerPacket) (rest : List UInt8) :
    decode (encode message ++ rest) = some (message, rest) :=
  decodeFramed_encodeFramed 2 0 encodeBody decodeBody message (decodeBody_encodeBody message) (encodeBody_length_lt message) rest

theorem encode_length_pos (message : ServerPacket) : (encode message).length > 0 := by
  unfold encode
  rw [encodeFramed_length]
  omega

end ServerPacket

end Omi.NasdaqUtpSnapshotUtpV30Server
